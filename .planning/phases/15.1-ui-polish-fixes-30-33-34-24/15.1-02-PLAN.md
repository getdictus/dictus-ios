---
phase: 15.1-ui-polish-fixes-30-33-34-24
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DictusCore/Sources/DictusCore/SharedKeys.swift
  - DictusApp/Services/SoundFeedbackService.swift
  - DictusApp/Views/SoundSettingsView.swift
  - DictusApp/Views/SettingsView.swift
  - DictusApp/DictationCoordinator.swift
  - Dictus.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - "#24"

must_haves:
  truths:
    - "Recording start plays a sound before WhisperKit takes audio session"
    - "Recording stop plays a sound after WhisperKit releases audio session"
    - "Recording cancel plays a different sound after cleanup"
    - "Sounds respect the physical silent switch"
    - "User can toggle sound feedback on/off in Settings > Sons"
    - "User can pick different sounds for start/stop/cancel from 29 WAV files"
    - "User can preview each sound with a play button in settings"
    - "Sound preferences persist across app launches via App Group"
  artifacts:
    - path: "DictusCore/Sources/DictusCore/SharedKeys.swift"
      provides: "4 new shared keys for sound preferences"
      contains: "soundFeedbackEnabled"
    - path: "DictusApp/Services/SoundFeedbackService.swift"
      provides: "AudioToolbox-based sound playback service"
      contains: "AudioServicesPlaySystemSound"
    - path: "DictusApp/Views/SoundSettingsView.swift"
      provides: "Sound settings sub-page with toggle + 3 pickers + preview"
      contains: "SoundSettingsView"
    - path: "DictusApp/Views/SettingsView.swift"
      provides: "Sons navigation row in Clavier section"
      contains: "Sons"
    - path: "DictusApp/DictationCoordinator.swift"
      provides: "Sound playback calls at start/stop/cancel lifecycle points"
      contains: "SoundFeedbackService"
  key_links:
    - from: "DictusApp/DictationCoordinator.swift"
      to: "DictusApp/Services/SoundFeedbackService.swift"
      via: "SoundFeedbackService.playRecordStart/Stop/Cancel() calls"
      pattern: "SoundFeedbackService\\.play"
    - from: "DictusApp/Services/SoundFeedbackService.swift"
      to: "DictusCore/Sources/DictusCore/SharedKeys.swift"
      via: "UserDefaults read using SharedKeys constants"
      pattern: "SharedKeys\\.sound"
    - from: "DictusApp/Views/SettingsView.swift"
      to: "DictusApp/Views/SoundSettingsView.swift"
      via: "NavigationLink to SoundSettingsView"
      pattern: "NavigationLink.*Sons"
---

<objective>
Implement recording sound feedback system (#24): a SoundFeedbackService using AudioServicesPlaySystemSound with the 29 existing WAV files, a SoundSettingsView for configuration (global toggle + 3 event pickers with preview), and integration into DictationCoordinator lifecycle hooks.

Purpose: Users get audio confirmation when recording starts, stops, or is cancelled -- essential feedback for a dictation app where eyes may be on a different app. Sounds must not conflict with WhisperKit's audio session.

Output: SoundFeedbackService.swift, SoundSettingsView.swift, updated SharedKeys, updated SettingsView, updated DictationCoordinator.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-ui-polish-fixes-30-33-34-24/15.1-CONTEXT.md
@.planning/phases/15.1-ui-polish-fixes-30-33-34-24/15.1-RESEARCH.md

@DictusCore/Sources/DictusCore/SharedKeys.swift
@DictusApp/DictationCoordinator.swift
@DictusApp/Views/SettingsView.swift
@DictusCore/Sources/DictusCore/HapticFeedback.swift

<interfaces>
<!-- SharedKeys.swift (current, append new keys after line 63): -->
<!-- public static let sourceAppScheme = "dictus.sourceAppScheme" -->
<!-- Add 4 new keys: soundFeedbackEnabled, recordStartSoundName, recordStopSoundName, recordCancelSoundName -->

<!-- DictationCoordinator.swift integration points: -->
<!-- func startDictation(fromURL: Bool = false) — line 213 -->
<!--   Play start sound BEFORE audioRecorder.configureAudioSession() (line 261) -->
<!--   Add ~200ms delay before proceeding to let sound play -->
<!-- func stopDictation() — line 350 -->
<!--   Play stop sound AFTER updateStatus(.transcribing) (line 393) -->
<!-- func cancelDictation() — line 486 -->
<!--   Play cancel sound AFTER cleanup, before updateStatus(.idle) (line 517) -->

<!-- SettingsView.swift insertion point: -->
<!-- Section("Clavier") { ... } — after haptics toggle (line 62) -->
<!-- Add NavigationLink("Sons") { SoundSettingsView() } -->

<!-- HapticFeedback.swift pattern to follow: -->
<!-- enum HapticFeedback { static func recordingStarted() ... } -->
<!-- SoundFeedbackService should mirror this enum-with-static-methods pattern -->

<!-- Available sound files (29 WAV in Dictus/Sounds/): -->
<!-- electronic_01a through electronic_01f, electronic_02a through electronic_02f -->
<!-- electronic_03a through electronic_03i, electronic_04a through electronic_04g -->
<!-- ui_chime_01 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SharedKeys + SoundFeedbackService + SoundSettingsView</name>
  <files>DictusCore/Sources/DictusCore/SharedKeys.swift, DictusApp/Services/SoundFeedbackService.swift, DictusApp/Views/SoundSettingsView.swift</files>
  <action>
    **Step 1 — SharedKeys (DictusCore):**
    Add 4 new keys to SharedKeys enum after `sourceAppScheme`:
    ```swift
    // MARK: - Sound Feedback
    public static let soundFeedbackEnabled = "dictus.soundFeedbackEnabled"
    public static let recordStartSoundName = "dictus.recordStartSoundName"
    public static let recordStopSoundName = "dictus.recordStopSoundName"
    public static let recordCancelSoundName = "dictus.recordCancelSoundName"
    ```

    **Step 2 — SoundFeedbackService (new file):**
    Create `DictusApp/Services/SoundFeedbackService.swift`. Follow the HapticFeedback.swift pattern (enum with static methods).

    ```swift
    import AudioToolbox
    import DictusCore
    ```

    Key implementation details:
    - `enum SoundFeedbackService` with `private static var cachedSounds: [String: SystemSoundID] = [:]`
    - `private static func isEnabled() -> Bool` reads `SharedKeys.soundFeedbackEnabled` from App Group UserDefaults. Default is `true` when key is absent.
    - `static func play(_ soundName: String)` — if empty name, return. Check cache first. If not cached, use `Bundle.main.url(forResource: soundName, withExtension: "wav")` to find the file, `AudioServicesCreateSystemSoundID` to register it, cache the ID, then `AudioServicesPlaySystemSound` to play. Guard on `kAudioServicesNoError` status.
    - `static func playRecordStart()` — reads `SharedKeys.recordStartSoundName` from App Group defaults, defaults to `"electronic_01a"`, calls `play()`.
    - `static func playRecordStop()` — reads `SharedKeys.recordStopSoundName`, defaults to `"ui_chime_01"`, calls `play()`.
    - `static func playRecordCancel()` — reads `SharedKeys.recordCancelSoundName`, defaults to `"electronic_02a"`, calls `play()`.
    - `static func availableSounds() -> [String]` — returns hardcoded sorted array of all 29 sound file names (without extension). This avoids Bundle directory enumeration complexity.

    AudioServicesPlaySystemSound respects the physical silent switch natively — no extra code needed.

    **Step 3 — SoundSettingsView (new file):**
    Create `DictusApp/Views/SoundSettingsView.swift`.

    Structure:
    - `struct SoundSettingsView: View`
    - 4 `@AppStorage` properties with App Group store:
      - `soundFeedbackEnabled` (Bool, default true)
      - `recordStartSoundName` (String, default "electronic_01a")
      - `recordStopSoundName` (String, default "ui_chime_01")
      - `recordCancelSoundName` (String, default "electronic_02a")
    - `var body: some View` → `List` in `NavigationStack` (or rely on parent NavigationStack):
      - **Section 1:** Toggle "Activer les sons" bound to `soundFeedbackEnabled`
      - **Section 2:** "Début d'enregistrement" — Picker (menu style or navigation style) bound to `recordStartSoundName` listing all 29 sounds by name. Next to the picker, a Button with speaker.wave.2 icon that calls `SoundFeedbackService.play(recordStartSoundName)` for preview.
      - **Section 3:** "Fin d'enregistrement" — same pattern for stop sound.
      - **Section 4:** "Annulation" — same pattern for cancel sound.
    - Sections 2-4 disabled when `soundFeedbackEnabled` is false.
    - `.navigationTitle("Sons")`
    - Each Picker shows sound names formatted for display: replace underscores with spaces, capitalize first letter. Example: "electronic_01a" becomes "Electronic 01a".

    Add both new Swift files to the Xcode project (DictusApp target) in project.pbxproj. SoundFeedbackService.swift goes in a Services group (create if needed). SoundSettingsView.swift goes in the Views group alongside SettingsView.swift.
  </action>
  <verify>
    <automated>cd DictusCore && swift test 2>&1 | tail -5</automated>
  </verify>
  <done>SharedKeys has 4 new sound preference keys. SoundFeedbackService.swift exists with play/playRecordStart/playRecordStop/playRecordCancel methods using AudioServicesPlaySystemSound. SoundSettingsView.swift exists with toggle + 3 pickers + preview buttons. Both files added to Xcode project. DictusCore tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sound playback into DictationCoordinator and Settings navigation</name>
  <files>DictusApp/DictationCoordinator.swift, DictusApp/Views/SettingsView.swift, Dictus.xcodeproj/project.pbxproj</files>
  <action>
    **Step 1 — SettingsView integration:**
    In SettingsView.swift, inside `Section("Clavier")`, after the haptics toggle (line ~62), add:
    ```swift
    NavigationLink("Sons") {
        SoundSettingsView()
    }
    ```

    **Step 2 — DictationCoordinator integration:**
    Add `// import is implicit — same target` comment for clarity.

    Three integration points with CRITICAL timing:

    **Start sound (in `startDictation()`):**
    Before `try? audioRecorder.configureAudioSession()` (line ~261 in the non-URL path, or equivalent location in the fromURL path), add:
    ```swift
    SoundFeedbackService.playRecordStart()
    ```
    Then add a 250ms delay before configureAudioSession to let the sound play before WhisperKit takes the audio session. Use:
    ```swift
    try? await Task.sleep(nanoseconds: 250_000_000)
    ```
    If `startDictation` is not async, use `DispatchQueue.main.asyncAfter(deadline: .now() + 0.25)` to wrap the configureAudioSession and everything after it. Check the function signature — if it's already in an async context or uses Task {}, integrate accordingly.

    **Stop sound (in `stopDictation()`):**
    After `updateStatus(.transcribing)` (line ~393), add:
    ```swift
    SoundFeedbackService.playRecordStop()
    ```
    At this point recording has stopped and WhisperKit has released the audio session — safe to play.

    **Cancel sound (in `cancelDictation()`):**
    Before `updateStatus(.idle)` (line ~517), add:
    ```swift
    SoundFeedbackService.playRecordCancel()
    ```
    After engine cleanup but before returning to idle state.

    **Step 3 — Verify sound files in build phase:**
    Check that the 29 WAV files in `Dictus/Sounds/` are included in DictusApp's "Copy Bundle Resources" build phase in project.pbxproj. If they're NOT listed, add them. Look for PBXResourcesBuildPhase for the DictusApp target and verify `.wav` file references exist. If Dictus/Sounds/ is a folder reference, it copies automatically; if individual file references, each must be in the build phase.
  </action>
  <verify>
    <automated>grep -c "SoundFeedbackService" DictusApp/DictationCoordinator.swift && grep "Sons" DictusApp/Views/SettingsView.swift && cd DictusCore && swift test 2>&1 | tail -3</automated>
  </verify>
  <done>DictationCoordinator calls SoundFeedbackService.playRecordStart() before audio session configuration (with 250ms delay), playRecordStop() after transcribing status, and playRecordCancel() before idle status. SettingsView has "Sons" NavigationLink in Clavier section. Sound WAV files confirmed in DictusApp bundle resources. DictusCore tests pass.</done>
</task>

</tasks>

<verification>
1. `grep "SoundFeedbackService" DictusApp/DictationCoordinator.swift` shows 3 calls (start/stop/cancel)
2. `grep "Sons" DictusApp/Views/SettingsView.swift` shows NavigationLink
3. `grep "soundFeedbackEnabled" DictusCore/Sources/DictusCore/SharedKeys.swift` shows new key
4. `ls DictusApp/Services/SoundFeedbackService.swift` confirms file exists
5. `ls DictusApp/Views/SoundSettingsView.swift` confirms file exists
6. `cd DictusCore && swift test` passes
</verification>

<success_criteria>
- SoundFeedbackService plays sounds via AudioServicesPlaySystemSound (no AVAudioPlayer)
- Start sound plays BEFORE WhisperKit audio session with ~250ms gap
- Stop/cancel sounds play AFTER WhisperKit releases session
- Settings > Clavier > Sons opens SoundSettingsView with toggle + 3 pickers + preview
- Sound preferences persist via App Group SharedKeys
- Silent switch is respected (AudioServicesPlaySystemSound handles natively)
- DictusCore tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-ui-polish-fixes-30-33-34-24/15.1-02-SUMMARY.md`
</output>
