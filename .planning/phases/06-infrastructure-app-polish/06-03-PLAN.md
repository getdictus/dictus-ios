---
phase: 06-infrastructure-app-polish
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - DictusApp/Onboarding/OnboardingView.swift
  - DictusApp/Onboarding/TestRecordingPage.swift
  - DictusApp/Onboarding/ModelDownloadPage.swift
  - DictusApp/Views/TestDictationView.swift
  - DictusApp/Views/RecordingView.swift
autonomous: false
requirements: [VIS-04, VIS-05, VIS-08]

must_haves:
  truths:
    - "User cannot swipe between onboarding steps — progression only happens via buttons after step completion"
    - "Test recording screen shows a large centered mic button with ambient waveform behind it"
    - "After stopping recording, transcription text fades in replacing the waveform — no card, simple transition"
    - "Haptic feedback fires on mic button tap and stop button tap"
    - "Model download step rewords the explanation and does NOT auto-advance — user sees completion, then taps Continuer"
    - "Test recording screen is reused from both onboarding step 5 and HomeView 'Tester la dictee' button"
  artifacts:
    - path: "DictusApp/Views/RecordingView.swift"
      provides: "Shared immersive recording screen"
      contains: "struct RecordingView"
    - path: "DictusApp/Onboarding/OnboardingView.swift"
      provides: "Step-controlled onboarding"
      contains: "switch currentPage"
  key_links:
    - from: "DictusApp/Onboarding/OnboardingView.swift"
      to: "DictusApp/Onboarding/TestRecordingPage.swift"
      via: "case 4: TestRecordingPage"
      pattern: "TestRecordingPage"
    - from: "DictusApp/Onboarding/TestRecordingPage.swift"
      to: "DictusApp/Views/RecordingView.swift"
      via: "RecordingView(mode: .onboarding)"
      pattern: "RecordingView"
    - from: "DictusApp/Views/HomeView.swift"
      to: "DictusApp/Views/RecordingView.swift"
      via: "NavigationLink to RecordingView"
      pattern: "RecordingView"
---

<objective>
Redesign the test recording screen as an immersive Voice Memos-style experience shared between onboarding and HomeView, fix the onboarding flow to block step progression, and improve the model download UX.

Purpose: The test recording screen is the user's first real interaction with dictation — it must feel polished and immersive. The onboarding flow must prevent skipping steps to ensure every prerequisite (mic, keyboard, model) is properly set up.

Output: A shared RecordingView used from both contexts, OnboardingView with programmatic-only step advancement (no swipe), and improved ModelDownloadPage UX.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-app-polish/06-CONTEXT.md
@.planning/phases/06-infrastructure-app-polish/06-RESEARCH.md
@DictusApp/Onboarding/OnboardingView.swift
@DictusApp/Onboarding/TestRecordingPage.swift
@DictusApp/Onboarding/ModelDownloadPage.swift
@DictusApp/Views/TestDictationView.swift
@DictusApp/Onboarding/KeyboardSetupPage.swift

<interfaces>
<!-- From DictusCore (after Plan 01 consolidation) -->
<!-- All design components accessed via `import DictusCore` -->

Key components available:
```swift
// Views
AnimatedMicButton(status: DictationStatus, action: () -> Void)
BrandWaveform(energyLevels: [Float], maxHeight: CGFloat)
ProcessingAnimation()
DictusLogo(height: CGFloat)

// Modifiers
.dictusGlass()

// Colors
Color.dictusBackground, .dictusAccent, .dictusRecording, .dictusSuccess

// Typography
Font.dictusHeading, .dictusBody, .dictusCaption, .dictusSubheading

// Haptics (from DictusCore/HapticFeedback.swift)
HapticFeedback.impact(.medium)
```

<!-- DictationCoordinator API -->
```swift
coordinator.status: DictationStatus  // .idle, .requested, .recording, .transcribing, .ready, .failed
coordinator.startDictation()
coordinator.stopDictation()
coordinator.resetStatus()
coordinator.lastResult: String?
coordinator.bufferEnergy: [Float]
coordinator.bufferSeconds: TimeInterval
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared RecordingView and rewire both consumers</name>
  <files>
    DictusApp/Views/RecordingView.swift
    DictusApp/Onboarding/TestRecordingPage.swift
    DictusApp/Views/TestDictationView.swift
    DictusApp/Views/HomeView.swift
  </files>
  <action>
    1. Rewrite `DictusApp/Views/RecordingView.swift` (file already exists but will be replaced) as the single shared immersive recording screen. Design per user decision:

       - **Layout**: Full-screen dark background (Color.dictusBackground). Large AnimatedMicButton centered vertically with generous bottom padding (~60pt from bottom). No navigation chrome — immersive feel.

       - **Recording state**: When recording, BrandWaveform renders as ambient background behind the mic button at 30% opacity, filling the available space. Show recording duration below the mic button. The mic button becomes a stop button (red circle with white square, same as current TestRecordingPage pattern).

       - **Transcription state**: When transcribing, show ProcessingAnimation centered where the waveform was.

       - **Result state**: Waveform fades out (withAnimation .easeOut), transcription text fades in at the same vertical position. No card wrapper — just the text with `.dictusBody` font. Show a "Terminer" / "Reessayer" button below.

       - **Haptics**: Call `HapticFeedback.impact(.medium)` on mic button tap (start recording) and on stop button tap (stop recording). Read `DictusCore/Sources/DictusCore/HapticFeedback.swift` first to confirm the API.

       - **Mode enum**: `enum RecordingMode { case onboarding, standalone }`. In `.onboarding` mode, show a "Terminer" button after transcription that calls `onComplete`. In `.standalone` mode, show "Nouvelle dictee" to reset and "Terminer" to pop navigation.

       - **Props**: `init(mode: RecordingMode, onComplete: (() -> Void)? = nil)`. Uses `@EnvironmentObject var coordinator: DictationCoordinator`.

    2. Update `DictusApp/Onboarding/TestRecordingPage.swift` to wrap RecordingView:
       ```swift
       struct TestRecordingPage: View {
           let onComplete: () -> Void
           var body: some View {
               RecordingView(mode: .onboarding, onComplete: onComplete)
           }
       }
       ```
       This thin wrapper preserves the onboarding page contract while delegating to the shared view.

    3. Rewrite `DictusApp/Views/TestDictationView.swift` to use RecordingView:
       ```swift
       struct TestDictationView: View {
           var body: some View {
               RecordingView(mode: .standalone)
           }
       }
       ```
       Remove the old multi-state mic button and transcription area code. The RecordingView handles everything.

    4. In HomeView.swift, verify the NavigationLink to TestDictationView still works. No changes needed there — TestDictationView is just a thin wrapper now.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild build -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - RecordingView.swift exists with immersive centered layout, mode enum, waveform-to-text fade transition, and haptic feedback
    - TestRecordingPage.swift is a thin wrapper calling RecordingView(mode: .onboarding)
    - TestDictationView.swift is a thin wrapper calling RecordingView(mode: .standalone)
    - DictusApp builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix onboarding blocking and model download UX</name>
  <files>
    DictusApp/Onboarding/OnboardingView.swift
    DictusApp/Onboarding/ModelDownloadPage.swift
  </files>
  <action>
    1. Rewrite `OnboardingView.swift` to replace TabView paging with a manual switch/case approach (per research recommendation):

       - Keep `@SceneStorage("onboarding_currentPage") private var currentPage: Int = 0` for persistence across scene phase changes.
       - Keep `@State private var completedSteps: Set<Int> = []`.
       - Replace the TabView with a Group/ZStack containing a switch on currentPage, showing only the current page.
       - Each page gets an `onNext` callback that calls `advanceToPage(n)`. The user can ONLY advance via the page's own button — no swiping.
       - Add slide transitions: `.transition(.asymmetric(insertion: .move(edge: .trailing), removal: .move(edge: .leading)))` with `.animation(.easeInOut(duration: 0.3), value: currentPage)`.
       - Add step indicator dots at the bottom: HStack of 5 Circle views, filled for current/completed steps, outlined for future. Use `.dictusAccent` for active, `.gray.opacity(0.3)` for inactive.
       - Keep `.interactiveDismissDisabled()` on the ZStack.

    2. Update `ModelDownloadPage.swift`:

       - Reword the title from "Telecharger un modele" to "Modele vocal".
       - Add explanatory text below the title: "Pour transcrire votre voix, Dictus a besoin d'un modele vocal. Le telechargement prend environ 1 minute." — using `.dictusBody` font, `.secondary` foreground, centered.
       - Change the download button text to "Installer le modele".
       - REMOVE the auto-advance logic (`DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) { onNext() }`). Per user decision: do NOT auto-advance. Let the user see the completion checkmark, then tap "Continuer" manually.
       - Keep the "Continuer" button that appears after download completes — this is the only way to advance.
       - The model pre-load (warmUp/loadModel) is already handled by `downloadModel()` which calls `WhisperKit(config)` with `prewarm: true, load: true`. No additional code needed.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild build -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - OnboardingView uses switch/case instead of TabView — no swiping between steps
    - Step indicator dots show progress at the bottom
    - ModelDownloadPage has reworded UX text and no auto-advance
    - Transitions between steps are animated with slide effect
    - DictusApp builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify onboarding flow and recording screen</name>
  <action>
    Immersive test recording screen (shared between onboarding step 5 and HomeView), onboarding flow with blocked progression (no swiping), and improved model download UX.
  </action>
  <how-to-verify>
    1. Build and run on simulator: `xcodebuild build -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16'`
    2. Reset onboarding state if needed (delete app from simulator, reinstall)
    3. Walk through onboarding:
       - Step 1 (Welcome): verify "Continuer" button advances
       - Try swiping left — should NOT advance to step 2
       - Step 2 (Mic): grant permission, advance
       - Step 3 (Keyboard): tap "J'ai ajoute le clavier"
       - Step 4 (Model): verify reworded text ("Modele vocal" title, explanatory text). Tap "Installer le modele". Wait for download. Verify NO auto-advance — user must tap "Continuer"
       - Step 5 (Test recording): verify immersive centered mic button, ambient waveform on recording, transcription fade-in after stop
    4. After onboarding, tap "Tester la dictee" on HomeView — verify same immersive recording screen appears
    5. Check step indicator dots are visible and show progress throughout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
DictusApp builds clean. Onboarding flow blocks swiping. Test recording screen is immersive with centered mic, ambient waveform, and fade-to-text transition. Same screen used from both onboarding and HomeView.
</verification>

<success_criteria>
1. User cannot swipe between onboarding steps — buttons only
2. Test recording screen matches Voice Memos-style: large centered mic, ambient waveform, text replaces waveform with fade
3. Haptic feedback on mic tap and stop tap
4. Model download step has clear French explanation, no auto-advance
5. Same RecordingView used from onboarding step 5 and HomeView
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-app-polish/06-03-SUMMARY.md`
</output>
