---
phase: 06-infrastructure-app-polish
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - DictusApp/Views/HomeView.swift
autonomous: true
requirements: [VIS-06, VIS-07]

must_haves:
  truths:
    - "HomeView shows only the logo + blue 'Dictus' text as title — no white navigation title bar"
    - "After onboarding completes with a downloaded model, HomeView shows 'Modele actif' card with 'Whisper Small' and size, not 'Telecharger un modele'"
    - "Model card displays human-readable name (e.g. 'Whisper Small') and size (e.g. '~500 Mo'), not raw identifier"
  artifacts:
    - path: "DictusApp/Views/HomeView.swift"
      provides: "Fixed home dashboard"
      contains: "ModelInfo.forIdentifier"
  key_links:
    - from: "DictusApp/Views/HomeView.swift"
      to: "DictusCore/Sources/DictusCore/ModelInfo.swift"
      via: "ModelInfo.forIdentifier()"
      pattern: "ModelInfo\\.forIdentifier"
---

<objective>
Fix the two HomeView visual bugs: remove the duplicate "Dictus" navigation title and fix the post-onboarding model state display.

Purpose: After onboarding, users should see a clean home screen with their active model correctly displayed — not a stale "download a model" prompt and not a redundant white title.

Output: Updated HomeView.swift with navigation title removed, model card using ModelInfo for display name and size, and correct isModelReady state after onboarding.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-app-polish/06-CONTEXT.md
@.planning/phases/06-infrastructure-app-polish/06-RESEARCH.md
@DictusApp/Views/HomeView.swift
@DictusCore/Sources/DictusCore/ModelInfo.swift

<interfaces>
<!-- From DictusCore/Sources/DictusCore/ModelInfo.swift -->
<!-- ModelInfo.forIdentifier() returns an optional ModelInfo with displayName and sizeLabel -->
<!-- These already exist — no new code needed in ModelInfo.swift -->

Expected API:
```swift
let info = ModelInfo.forIdentifier("openai_whisper-small")
info?.displayName  // "Small"
info?.sizeLabel    // "~500 Mo" or similar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate navigation title and fix model card display</name>
  <files>DictusApp/Views/HomeView.swift</files>
  <action>
    1. Remove `.navigationTitle("Dictus")` at line 42. The `logoSection` (DictusLogo + blue "Dictus" text) already serves as the page title. Removing the navigation title eliminates the white title doublon.

    2. Fix modelStatusCard to use ModelInfo for human-readable display:
       - Replace `Text(modelName)` with `Text("Whisper \(info?.displayName ?? modelName)")` using `ModelInfo.forIdentifier(modelName)`.
       - Add model size below the name: `if let size = info?.sizeLabel { Text(size) }` with `.font(.dictusCaption)` and `.foregroundColor(.secondary)`.

    3. Fix post-onboarding state bug: The root cause is that ModelDownloadPage uses its own `@StateObject private var modelManager = ModelManager()` — a separate instance. When the user returns to HomeView after onboarding, HomeView's modelManager may have stale state.
       - Add `.onAppear { modelManager.loadState() }` to the HomeView body so the model state is refreshed every time HomeView appears. This ensures `isModelReady` reflects the actual downloaded model state.
       - Read ModelManager.swift to confirm `loadState()` exists and refreshes `downloadedModels` and `activeModel`. If it doesn't exist, add a method that re-reads from App Group defaults.

    4. Remove any visual side-band artifacts. Check if there are any stray background modifiers or edge-to-edge issues.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild build -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `.navigationTitle("Dictus")` removed from HomeView
    - Model card shows "Whisper Small" (not "openai_whisper-small") with size label
    - HomeView refreshes model state on appear via `loadState()` — no stale "download a model" after onboarding
    - DictusApp builds without errors
  </done>
</task>

</tasks>

<verification>
DictusApp builds clean. HomeView.swift contains no `.navigationTitle` call. Model card uses `ModelInfo.forIdentifier()` for display name.
</verification>

<success_criteria>
1. No duplicate "Dictus" title visible — only the logo section with blue text
2. After onboarding, HomeView correctly shows the active model with friendly name and size
3. "Telecharger un modele" card only appears when genuinely no model is downloaded
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-app-polish/06-02-SUMMARY.md`
</output>
