---
phase: 34-silent-insertion-fix
plan: 04
type: execute
wave: 3
depends_on: ["34-01", "34-02", "34-03"]
files_modified:
  - .planning/phases/34-silent-insertion-fix/test-matrix.md
autonomous: false
requirements: [STAB-01]

must_haves:
  truths:
    - "A test-matrix.md artifact exists in the phase directory documenting the 50-run manual protocol, the #118 repro matrix, and the 3-path regression checklist — the primary evidence artifact for closing issue #118"
    - "50 consecutive real-device dictations are executed and logged in the matrix with per-run pass/fail + probe log evidence (STAB-01 success criterion #1)"
    - "All four #118 repro scenarios (warm same-app, cold start, backgrounded keyboard, rapid chained) are exercised on a real device and marked with probe log evidence (STAB-01 success criterion #2)"
    - "All three insertion paths (warm Darwin, cold start Audio Bridge, chained recording) are exercised on a real device and recorded in the regression checklist (STAB-01 success criterion #3)"
    - "Zero silent drops across all 50 runs + 4 repro scenarios + 3 regression paths — verified by log review showing keyboardInsertProbe success for every attempt and ZERO keyboardInsertFailed entries in happy-path runs"
  artifacts:
    - path: ".planning/phases/34-silent-insertion-fix/test-matrix.md"
      provides: "The 50-run protocol document, results log, repro matrix, and regression checklist that formally closes STAB-01"
      contains: "## 50-Run Protocol"
  key_links:
    - from: "test-matrix.md Results Log"
      to: "PersistentLog keyboardInsertProbe entries"
      via: "per-run log evidence snippet (sessionID, path, transcriptionCount, darwinToInsertMs)"
      pattern: "keyboardInsertProbe"
    - from: "test-matrix.md #118 Repro Matrix"
      to: "GitHub issue #118 original repro scenarios"
      via: "scenario-by-scenario PASS/FAIL with probe log evidence"
      pattern: "warm same-app|cold start|backgrounded keyboard|rapid chained"
    - from: "test-matrix.md 3-Path Regression Checklist"
      to: "InsertTranscriptionHelper path argument (warmDarwin / coldStartBridge)"
      via: "log grep confirming each path was exercised"
      pattern: "path=warmDarwin|path=coldStartBridge"
---

<objective>
Create and execute the manual verification protocol that formally closes STAB-01. Produces `.planning/phases/34-silent-insertion-fix/test-matrix.md` containing the 50-Run Protocol, the #118 Repro Matrix, the 3-Path Regression Checklist, and the Results Log populated from real-device testing on an iPhone running the build that ships plans 01/02/03.

Purpose: Per CONTEXT.md decision (locked): the 50-run success criterion is verified by probe logs, not user perception. Automated XCUITest is explicitly deferred. This plan is the human-driven evidence-gathering exercise that produces the artifact referenced from the PR description when issue #118 is closed.
Output: One new file — `test-matrix.md` — scaffolded in Task 1 and populated with real-device results across Tasks 2-4.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-silent-insertion-fix/34-CONTEXT.md
@.planning/phases/34-silent-insertion-fix/34-VALIDATION.md
@.planning/phases/34-silent-insertion-fix/34-01-PLAN.md
@.planning/phases/34-silent-insertion-fix/34-03-PLAN.md

<interfaces>
<!-- Probe log event names shipped by Plan 01 / emitted by Plan 03 helper. -->
<!-- The executor reads these from PersistentLog exports (log-dump UX) to populate the results log. -->

LogEvent cases the matrix grep-checks for:
- `keyboardInsertProbe` — emitted on every insertion attempt (success or retry trigger)
  format: `path=<warmDarwin|coldStartBridge> sessionID=<id> attempt=<N> transcriptionCount=<N> hasFullAccess=<bool> hasTextBefore=<bool> hasTextAfter=<bool> beforeCount=<N> afterCount=<N> keyboardVisible=<bool> darwinToInsertMs=<N>`
- `keyboardInsertRetry` — emitted when an attempt fails and another is scheduled
  format: `path=<...> sessionID=<...> attempt=<N> reason=<proxyNil|contextUnavailable|silentDrop|deltaMismatch|proxyDead>`
- `keyboardInsertFailed` — emitted on terminal failure (after exhausting retries or noFullAccess)
  format: `path=<...> sessionID=<...> totalAttempts=<N> finalReason=<...>`

InsertionPath values (from InsertTranscriptionHelper.swift):
- `warmDarwin` — keyboard was already up when Darwin notification arrived
- `coldStartBridge` — cold-start Audio Bridge triggered insertion (SharedKeys.coldStartActive == true)

Evidence rule (from CONTEXT.md):
- Success = exactly one `keyboardInsertProbe` per run with `attempt=0` and ZERO `keyboardInsertRetry`/`keyboardInsertFailed` follow-ups for that sessionID.
- Failure in happy path = ANY `keyboardInsertFailed` with matching sessionID.

Privacy rule (non-negotiable, enforced in Plan 01):
- Logs MUST NOT contain raw transcription text. Evidence snippets pasted into the matrix MUST be counts/booleans/identifiers only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold test-matrix.md with all four sections</name>
  <files>.planning/phases/34-silent-insertion-fix/test-matrix.md</files>
  <read_first>
    .planning/phases/34-silent-insertion-fix/34-CONTEXT.md,
    .planning/phases/34-silent-insertion-fix/34-VALIDATION.md,
    .planning/phases/34-silent-insertion-fix/34-03-PLAN.md
  </read_first>
  <action>
Create `.planning/phases/34-silent-insertion-fix/test-matrix.md` with the exact structure below. This is a scaffolding task — the results columns stay empty (status `⬜ pending`) and get filled by Tasks 2-4. Do NOT run any device tests in this task; the executor's job here is purely to create the document skeleton.

Required top-level sections (in this order, with these exact H2 headings):
1. `## 50-Run Protocol` — describes the run methodology
2. `## #118 Repro Matrix` — four-scenario checklist
3. `## 3-Path Regression Checklist` — warm Darwin / cold start Audio Bridge / chained recording
4. `## Results Log` — the 50-run results table

File contents:

```markdown
---
phase: 34-silent-insertion-fix
artifact: manual-verification
requirement: STAB-01
status: in-progress
created: 2026-04-15
---

# Phase 34 — Manual Verification Test Matrix

> Artifact that formally closes STAB-01. Referenced from the fix PR description
> and from issue #118 when we mark it resolved.
>
> **Evidence discipline:** every PASS/FAIL entry cites a `keyboardInsertProbe`
> / `keyboardInsertRetry` / `keyboardInsertFailed` log line. No raw
> transcription text is ever pasted — logs contain counts/booleans/timings only
> (enforced in Plan 01, verified in LogPrivacyTests).
>
> **Device:** real iPhone running the build that ships plans 01 + 02 + 03.
> Simulator does NOT count — `textDocumentProxy` behaves differently and the
> bug is real-device-only per issue #118.

---

## 50-Run Protocol

**Goal:** 50 consecutive real-device dictations with zero silent drops
(STAB-01 success criterion #1).

**Target apps (rotate across the 50 runs):**
- Notes — long existing document (non-empty field)
- Notes — new empty note (empty field edge case)
- Messages — chat input (multi-line, autocorrect active)
- Safari — address bar (short field, autocomplete active)

**Scenario distribution across the 50 runs:**
- Runs 01-30: **warm path** — keyboard already up, dictate, wait for insert
- Runs 31-40: **cold start** — force-kill DictusApp via app switcher before each run
- Runs 41-50: **rapid chained** — 2-3 dictations back-to-back in same app

**Per-run procedure:**
1. Note the `sessionID` (emitted in probe log).
2. Dictate a phrase (≥ 5 words so `transcriptionCount` is non-trivial).
3. Wait for insertion OR failure banner.
4. Export logs via the existing log-dump UX.
5. Record: `PASS` if text landed AND exactly one `keyboardInsertProbe` with
   zero `keyboardInsertRetry`/`keyboardInsertFailed` for that sessionID.
   `FAIL` otherwise.
6. Privacy check (spot-check every 10 runs): grep the exported log for a
   distinctive dictated word. Expected: ZERO hits.

**Pass bar:** 50/50 PASS. Any FAIL is a blocker — diagnose, fix, rerun
the entire 50-run protocol. Partial success does not close STAB-01.

---

## #118 Repro Matrix

**Goal:** every scenario originally reported in issue #118 now produces a
visible outcome (success OR loud-fail UX) — no silent drops (STAB-01 success
criterion #2).

| # | Scenario | Steps | Expected | Result | Log Evidence |
|---|----------|-------|----------|--------|--------------|
| 1 | Warm same-app dictation | Keyboard already up in Notes, dictate short phrase | Text lands, 1× probe `path=warmDarwin attempt=0` | ⬜ pending | — |
| 2 | Cold start | Force-kill DictusApp, open Messages, tap mic, dictate | Text lands, 1× probe `path=coldStartBridge attempt=0` | ⬜ pending | — |
| 3 | Backgrounded keyboard | Switch app while dictation in flight, return | Either text lands OR banner+DI failure + home card updated | ⬜ pending | — |
| 4 | Rapid chained recordings | Three back-to-back dictations in Notes | 3× text landed, 3× probe entries with distinct sessionIDs | ⬜ pending | — |

---

## 3-Path Regression Checklist

**Goal:** confirm each of the three insertion code paths is exercised by the
new helper and observed in logs (STAB-01 success criterion #3).

| Path | How exercised | Expected log signature | Result | Notes |
|------|---------------|------------------------|--------|-------|
| warm Darwin | Keyboard up when Darwin notification arrives | `keyboardInsertProbe path=warmDarwin` | ⬜ pending | — |
| cold start Audio Bridge | DictusApp killed, keyboard bridges via URL scheme, Darwin arrives post-wake | `keyboardInsertProbe path=coldStartBridge` | ⬜ pending | — |
| chained recording | Second dictation starts before first state fully resets | `keyboardInsertProbe` with two distinct `sessionID` values in quick succession | ⬜ pending | — |

---

## Results Log

**Device:** <fill in: iPhone model + iOS version>
**Build:** <fill in: version + build number from Info.plist>
**Tester:** <fill in: name>
**Date window:** <fill in: start — end>

| Run | App | Scenario | sessionID | transcriptionCount | darwinToInsertMs | Result | Evidence |
|-----|-----|----------|-----------|--------------------|------------------|--------|----------|
| 01 | — | warm | — | — | — | ⬜ pending | — |
| 02 | — | warm | — | — | — | ⬜ pending | — |
| 03 | — | warm | — | — | — | ⬜ pending | — |
| 04 | — | warm | — | — | — | ⬜ pending | — |
| 05 | — | warm | — | — | — | ⬜ pending | — |
| 06 | — | warm | — | — | — | ⬜ pending | — |
| 07 | — | warm | — | — | — | ⬜ pending | — |
| 08 | — | warm | — | — | — | ⬜ pending | — |
| 09 | — | warm | — | — | — | ⬜ pending | — |
| 10 | — | warm | — | — | — | ⬜ pending | — |
| 11 | — | warm | — | — | — | ⬜ pending | — |
| 12 | — | warm | — | — | — | ⬜ pending | — |
| 13 | — | warm | — | — | — | ⬜ pending | — |
| 14 | — | warm | — | — | — | ⬜ pending | — |
| 15 | — | warm | — | — | — | ⬜ pending | — |
| 16 | — | warm | — | — | — | ⬜ pending | — |
| 17 | — | warm | — | — | — | ⬜ pending | — |
| 18 | — | warm | — | — | — | ⬜ pending | — |
| 19 | — | warm | — | — | — | ⬜ pending | — |
| 20 | — | warm | — | — | — | ⬜ pending | — |
| 21 | — | warm | — | — | — | ⬜ pending | — |
| 22 | — | warm | — | — | — | ⬜ pending | — |
| 23 | — | warm | — | — | — | ⬜ pending | — |
| 24 | — | warm | — | — | — | ⬜ pending | — |
| 25 | — | warm | — | — | — | ⬜ pending | — |
| 26 | — | warm | — | — | — | ⬜ pending | — |
| 27 | — | warm | — | — | — | ⬜ pending | — |
| 28 | — | warm | — | — | — | ⬜ pending | — |
| 29 | — | warm | — | — | — | ⬜ pending | — |
| 30 | — | warm | — | — | — | ⬜ pending | — |
| 31 | — | cold start | — | — | — | ⬜ pending | — |
| 32 | — | cold start | — | — | — | ⬜ pending | — |
| 33 | — | cold start | — | — | — | ⬜ pending | — |
| 34 | — | cold start | — | — | — | ⬜ pending | — |
| 35 | — | cold start | — | — | — | ⬜ pending | — |
| 36 | — | cold start | — | — | — | ⬜ pending | — |
| 37 | — | cold start | — | — | — | ⬜ pending | — |
| 38 | — | cold start | — | — | — | ⬜ pending | — |
| 39 | — | cold start | — | — | — | ⬜ pending | — |
| 40 | — | cold start | — | — | — | ⬜ pending | — |
| 41 | — | chained | — | — | — | ⬜ pending | — |
| 42 | — | chained | — | — | — | ⬜ pending | — |
| 43 | — | chained | — | — | — | ⬜ pending | — |
| 44 | — | chained | — | — | — | ⬜ pending | — |
| 45 | — | chained | — | — | — | ⬜ pending | — |
| 46 | — | chained | — | — | — | ⬜ pending | — |
| 47 | — | chained | — | — | — | ⬜ pending | — |
| 48 | — | chained | — | — | — | ⬜ pending | — |
| 49 | — | chained | — | — | — | ⬜ pending | — |
| 50 | — | chained | — | — | — | ⬜ pending | — |

---

## Summary

**50-Run result:** <fill in: X/50 PASS>
**#118 Repro Matrix:** <fill in: X/4 PASS>
**3-Path Regression:** <fill in: X/3 PASS>
**Privacy audit:** <fill in: PASS/FAIL — zero raw-text leaks in logs>

**STAB-01 status:** <fill in: resolved | blocked | partial — reason>
```

Do NOT populate any results — this task is scaffolding only. The `⬜ pending` placeholders and `—` cells are filled by subsequent tasks.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-silent-insertion-fix/test-matrix.md &amp;&amp; grep -c "^## " .planning/phases/34-silent-insertion-fix/test-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/34-silent-insertion-fix/test-matrix.md` exists
    - `grep -c "^## 50-Run Protocol$" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1
    - `grep -c "^## #118 Repro Matrix$" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1
    - `grep -c "^## 3-Path Regression Checklist$" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1
    - `grep -c "^## Results Log$" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1
    - `grep -c "^| [0-9]\+ |" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 50 (results table has 50 run rows)
    - `grep -c "warm same-app\|cold start\|backgrounded keyboard\|rapid chained" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 4 (all #118 scenarios named)
    - `grep -c "warm Darwin\|cold start Audio Bridge\|chained recording" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 3 (all 3 regression paths named)
    - `grep -c "keyboardInsertProbe\|keyboardInsertRetry\|keyboardInsertFailed" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 3 (probe event names referenced)
  </acceptance_criteria>
  <done>test-matrix.md exists with the four required section headers, a 50-row results table, the #118 repro matrix naming all four scenarios, the 3-path regression checklist, and probe event names referenced for evidence discipline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the 50-run protocol on real iPhone and record results</name>
  <what-built>Plans 01, 02, 03 shipped a build with the InsertTranscriptionHelper, the home-card recovery surface, and three privacy-safe probe LogEvent cases. The scaffolded test-matrix.md is waiting for real-device results.</what-built>
  <how-to-verify>
1. **Install the build** on a real iPhone. Confirm from Settings > General > VPN & Device Management (or from the app) that the build includes plans 01/02/03 — the new probe events are in the log export when you do a quick test dictation.

2. **Open `test-matrix.md`** and fill in the header fields in the Results Log: Device (iPhone model + iOS version), Build (version + build number from Info.plist — bump build per MEMORY.md convention if needed), Tester (Pierre), Date window (start date).

3. **Runs 01-30 (warm path):** Keyboard already up in a host app (rotate Notes / Messages / Safari address bar per the rotation spec in the `## 50-Run Protocol` section). Dictate a phrase ≥ 5 words. Wait for insertion. Export logs via the existing log-dump UX. For each run fill in: App, sessionID, transcriptionCount, darwinToInsertMs, Result (✅ PASS if text landed AND exactly one `keyboardInsertProbe` for that sessionID with zero `keyboardInsertRetry` / `keyboardInsertFailed` follow-ups — ❌ FAIL otherwise), Evidence (one-line log snippet: the probe line with raw text REDACTED).

4. **Runs 31-40 (cold start):** Before EACH run: app-switcher force-kill DictusApp. Open host app (Messages preferred). Tap the mic in the keyboard. Dictate. Tap "< Back" to return to Messages. Record as above — evidence line MUST show `path=coldStartBridge`.

5. **Runs 41-50 (rapid chained):** In a single Notes document, dictate three phrases back-to-back (new mic tap immediately after previous result). Record three separate rows, distinct sessionIDs.

6. **Privacy spot-checks:** After runs 10, 20, 30, 40, 50 — grep the exported log for a distinctive word you dictated ("écrevisse", "caparaçon", anything rare). Expected: ZERO hits. Note in Summary section at end of matrix.

7. **Fill the Summary section** at the bottom of test-matrix.md with X/50 PASS count.

8. **Pass bar:** 50/50. Any FAIL is a blocker — open a new issue, stop execution, do NOT mark STAB-01 resolved.
  </how-to-verify>
  <read_first>
    .planning/phases/34-silent-insertion-fix/test-matrix.md,
    .planning/phases/34-silent-insertion-fix/34-CONTEXT.md,
    .planning/phases/34-silent-insertion-fix/34-03-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `grep -c "^| 0[1-9] \||^| [1-4][0-9] \||^| 50 \|" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 50 (every run row has been edited — sessionID column is no longer just `—`)
    - `grep -cE "\| ✅ PASS \|" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 50 (all runs passed — any FAIL blocks completion)
    - `grep -cE "\| ❌ FAIL \|" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 0
    - `grep -c "keyboardInsertProbe" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 50 (evidence snippets cite probe events)
    - `grep -c "path=coldStartBridge" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 10 (runs 31-40 exercised the cold-start path)
    - `grep -c "path=warmDarwin" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 30 (runs 01-30 exercised the warm path)
    - Summary section reports `50/50 PASS` AND `Privacy audit: PASS`
    - Tester has confirmed verbally: zero silent drops observed across all 50 runs
  </acceptance_criteria>
  <resume-signal>Type "approved" if 50/50 PASS and privacy audit clean. If ANY run failed, describe the run number, scenario, host app, and paste the `keyboardInsertFailed` log line (with sessionID). A single FAIL blocks this task — we diagnose and rerun the full 50 before proceeding.</resume-signal>
  <done>test-matrix.md Results Log shows 50/50 PASS with probe log evidence per run; privacy audit confirms zero raw-text leaks; Summary section filled in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute #118 repro matrix (4 scenarios) on real device</name>
  <what-built>The 50-run protocol (Task 2) confirmed baseline happy-path reliability. Task 3 targets the specific scenarios issue #118 originally reported — to formally close that issue with documented evidence per scenario.</what-built>
  <how-to-verify>
Open `test-matrix.md` and fill in the four rows under `## #118 Repro Matrix`. For each scenario: execute the steps in the "Steps" column, observe the outcome, paste a one-line log snippet into the "Log Evidence" column (redact any raw text), and set Result to ✅ PASS or ❌ FAIL.

1. **Scenario 1 — Warm same-app dictation:**
   - Steps: keyboard already up in Notes, dictate "this is the first warm test".
   - Expected: text appears in Notes. Log shows `keyboardInsertProbe path=warmDarwin attempt=0 hasTextAfter=true`.
   - Evidence line: paste the probe line (RAW TEXT REDACTED).

2. **Scenario 2 — Cold start:**
   - Steps: force-kill DictusApp via app switcher. Open Messages. Tap mic in keyboard. Dictate "this is the cold start test". Tap "< Back" to return to Messages.
   - Expected: text appears in Messages field. Log shows `keyboardInsertProbe path=coldStartBridge attempt=0`.
   - Evidence line: probe line with `path=coldStartBridge`.

3. **Scenario 3 — Backgrounded keyboard:**
   - Steps: in Notes, start dictation. While recording, swipe home to background DictusApp. Wait for transcription to land (via Audio Bridge). Return to Notes.
   - Expected EITHER: text landed in Notes AND probe entry exists, OR banner+DI failure triggered AND DictusApp home card shows the transcription. Both outcomes count as PASS — the failure scenario is what STAB-01 explicitly covers with the loud-fail UX.
   - Evidence line: probe or `keyboardInsertFailed` entry + note "home card showed text".

4. **Scenario 4 — Rapid chained recordings:**
   - Steps: in Notes, dictate three short phrases back-to-back (mic tap immediately after each insertion).
   - Expected: all three texts land in Notes. Log shows three probe entries with three distinct sessionIDs.
   - Evidence line: three probe lines (one per sessionID), or a single "3× keyboardInsertProbe with sessionIDs=A,B,C" summary.

Pass bar: 4/4 PASS. Scenario 3 is the tricky one — if insertion silently drops and the banner+home card do NOT fire, that's a FAIL (STAB-01 explicitly mandates loud-fail UX on failure).
  </how-to-verify>
  <read_first>
    .planning/phases/34-silent-insertion-fix/test-matrix.md,
    .planning/phases/34-silent-insertion-fix/34-CONTEXT.md,
    .planning/phases/34-silent-insertion-fix/34-02-SUMMARY.md,
    .planning/phases/34-silent-insertion-fix/34-03-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - All four rows of the `## #118 Repro Matrix` table have a non-placeholder Result (no `⬜ pending` remaining in the matrix)
    - `grep -cE "\| ✅ PASS \|" .planning/phases/34-silent-insertion-fix/test-matrix.md` increased by 4 from Task 2 baseline (4 additional PASS cells from this task)
    - `grep -c "path=coldStartBridge" .planning/phases/34-silent-insertion-fix/test-matrix.md` increased (scenario 2 added at least 1 evidence line)
    - `grep -iE "home card|DictusApp home" .planning/phases/34-silent-insertion-fix/test-matrix.md` returns at least 1 match (scenario 3 notes the recovery surface behavior)
    - No `⬜ pending` remains in the `## #118 Repro Matrix` section
    - Summary `#118 Repro Matrix:` row reports `4/4 PASS`
  </acceptance_criteria>
  <resume-signal>Type "approved" if 4/4 PASS. If any scenario FAILs, describe which (number + steps that broke) and paste the relevant log line.</resume-signal>
  <done>#118 Repro Matrix shows 4/4 PASS with per-scenario log evidence; Summary section updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute 3-path regression checklist and finalize matrix</name>
  <what-built>Tasks 2 and 3 already exercised all three paths implicitly. Task 4 is the explicit cross-check: confirm each of the three code paths (warm Darwin, cold start Audio Bridge, chained recording) was observed AT LEAST ONCE in the logs, and mark the regression checklist rows accordingly.</what-built>
  <how-to-verify>
Open `test-matrix.md` and fill the three rows in `## 3-Path Regression Checklist`:

1. **warm Darwin path:**
   - Verify: at least one row in the Results Log OR the #118 Repro Matrix cites `path=warmDarwin` in its Evidence column.
   - Set Result: ✅ PASS if observed, ❌ FAIL otherwise.
   - Notes: record the run number (e.g. "Run 05" or "Repro #1") that serves as the proof citation.

2. **cold start Audio Bridge path:**
   - Verify: at least one row cites `path=coldStartBridge`. The Task 2 cold-start runs (31-40) or the Task 3 Scenario 2 entry both qualify.
   - Set Result: ✅ PASS / ❌ FAIL.
   - Notes: cite the proof run.

3. **chained recording path:**
   - Verify: at least one set of 3 probe entries with 3 distinct sessionIDs appearing in quick succession (timestamps within seconds). Task 2 runs 41-50 or Task 3 Scenario 4 both qualify.
   - Set Result: ✅ PASS / ❌ FAIL.
   - Notes: cite the proof run / sessionID triplet.

Finally, **fill the Summary section at the bottom of `test-matrix.md`:**
- `50-Run result: X/50 PASS`
- `#118 Repro Matrix: X/4 PASS`
- `3-Path Regression: X/3 PASS`
- `Privacy audit: PASS/FAIL`
- `STAB-01 status: resolved | blocked | partial — reason`

Change the frontmatter `status:` field from `in-progress` to `complete` (or `blocked` if any task above FAILED).
  </how-to-verify>
  <read_first>
    .planning/phases/34-silent-insertion-fix/test-matrix.md
  </read_first>
  <acceptance_criteria>
    - No `⬜ pending` remains anywhere in `.planning/phases/34-silent-insertion-fix/test-matrix.md`
    - `grep -c "^| warm Darwin " .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1 AND that row contains `✅ PASS`
    - `grep -c "^| cold start Audio Bridge " .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1 AND that row contains `✅ PASS`
    - `grep -c "^| chained recording " .planning/phases/34-silent-insertion-fix/test-matrix.md` returns 1 AND that row contains `✅ PASS`
    - Summary section contains `50-Run result:` with a concrete ratio (not `<fill in:>`)
    - Summary section contains `#118 Repro Matrix:` with a concrete ratio
    - Summary section contains `3-Path Regression:` with a concrete ratio
    - Summary section contains `STAB-01 status: resolved` (if all prior tasks passed) OR `blocked` / `partial` with a written reason
    - Frontmatter `status:` field is `complete` or `blocked` (not `in-progress`)
  </acceptance_criteria>
  <resume-signal>Type "approved" if 3/3 regression paths confirmed AND summary section reflects overall STAB-01 status honestly (resolved / blocked / partial). If any path cannot be cited from logs, describe why and the matrix stays in `blocked` state.</resume-signal>
  <done>test-matrix.md fully populated: all tables non-pending, all three regression paths confirmed with log citations, summary section reflects final STAB-01 disposition, frontmatter status advanced.</done>
</task>

</tasks>

<verification>
Final state check — run from repo root:

```bash
# No pending placeholders remain
grep -c "⬜ pending" .planning/phases/34-silent-insertion-fix/test-matrix.md
# Expected: 0

# All 50 runs passed
grep -cE "\| ✅ PASS \|" .planning/phases/34-silent-insertion-fix/test-matrix.md
# Expected: ≥ 57 (50 runs + 4 repro + 3 regression)

# All three paths cited
grep -cE "path=warmDarwin|path=coldStartBridge" .planning/phases/34-silent-insertion-fix/test-matrix.md
# Expected: ≥ 40 (30 warm + 10 cold start at minimum)

# Frontmatter advanced
grep -E "^status: (complete|blocked)" .planning/phases/34-silent-insertion-fix/test-matrix.md
# Expected: 1 match
```

Expected final state: `STAB-01 status: resolved` in the Summary section AND frontmatter `status: complete`. Any other state (blocked / partial) means the fix does not ship — diagnose and reopen prior plans as needed.
</verification>

<success_criteria>
- `.planning/phases/34-silent-insertion-fix/test-matrix.md` exists with all four required H2 sections
- 50-Run Results Log: 50/50 ✅ PASS with per-run probe log evidence (STAB-01 success criterion #1)
- #118 Repro Matrix: 4/4 ✅ PASS across warm / cold start / backgrounded / chained (STAB-01 success criterion #2)
- 3-Path Regression Checklist: 3/3 ✅ PASS for warm Darwin / cold start Audio Bridge / chained recording (STAB-01 success criterion #3)
- Privacy audit: zero raw-text leaks confirmed in exported logs
- Summary section honestly reports final STAB-01 disposition (resolved / blocked / partial with reason)
- Frontmatter `status:` advanced from `in-progress` to `complete` or `blocked`
</success_criteria>

<output>
After completion, create `.planning/phases/34-silent-insertion-fix/34-04-SUMMARY.md` documenting: final 50-run tally, per-scenario #118 results, 3-path regression confirmation, any FAILs observed and how they were dispositioned, privacy audit outcome, and the final STAB-01 status. Reference `test-matrix.md` as the canonical evidence artifact for the PR description.
</output>
