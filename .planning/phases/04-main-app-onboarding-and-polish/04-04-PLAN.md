---
phase: 04-main-app-onboarding-and-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DictusApp/Onboarding/WelcomePage.swift
  - DictusApp/Onboarding/KeyboardSetupPage.swift
  - DictusApp/Onboarding/TestRecordingPage.swift
  - DictusApp/Onboarding/OnboardingView.swift
  - DictusApp/Design/BrandWaveform.swift
  - DictusApp/Design/DictusLogo.swift
  - DictusApp/Views/ModelManagerView.swift
  - DictusApp/Views/SettingsView.swift
autonomous: true
gap_closure: true
requirements: [APP-01, APP-03, DSN-01, DSN-04]

must_haves:
  truths:
    - "Welcome page shows animated multi-bar waveform, not static logo"
    - "All onboarding buttons have white text on colored backgrounds in both light and dark mode"
    - "Returning from iOS Settings during keyboard setup does not reset onboarding to step 1"
    - "BrandWaveform bars are visible in light mode (gray) and dark mode (white)"
    - "Models and Settings tabs have glass card styling matching Home tab"
  artifacts:
    - path: "DictusApp/Onboarding/WelcomePage.swift"
      provides: "Animated BrandWaveform on welcome step"
      contains: "BrandWaveform"
    - path: "DictusApp/Onboarding/OnboardingView.swift"
      provides: "Persistent page state via @SceneStorage"
      contains: "@SceneStorage"
    - path: "DictusApp/Design/BrandWaveform.swift"
      provides: "Adaptive bar colors for light/dark mode"
      contains: "colorScheme"
  key_links:
    - from: "DictusApp/Onboarding/WelcomePage.swift"
      to: "DictusApp/Design/BrandWaveform.swift"
      via: "BrandWaveform view usage"
      pattern: "BrandWaveform\\("
    - from: "DictusApp/Onboarding/OnboardingView.swift"
      to: "SwiftUI SceneStorage"
      via: "@SceneStorage property wrapper"
      pattern: "@SceneStorage"
---

<objective>
Fix 7 UAT issues across onboarding flow and app-wide glass styling.

Purpose: Close UAT gaps from Tests 3 and 6 — onboarding has static waveform, poor button contrast, state reset, invisible bars in light mode; Models and Settings tabs lack glass effect.
Output: All onboarding steps work correctly in light/dark mode; glass styling consistent across all tabs.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/onboarding-uat-5-issues.md
@.planning/debug/missing-glass-effect-cards.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix onboarding — animated waveform, button contrast, persistent state, adaptive colors</name>
  <files>
    DictusApp/Onboarding/WelcomePage.swift,
    DictusApp/Onboarding/KeyboardSetupPage.swift,
    DictusApp/Onboarding/TestRecordingPage.swift,
    DictusApp/Onboarding/OnboardingView.swift,
    DictusApp/Design/BrandWaveform.swift,
    DictusApp/Design/DictusLogo.swift
  </files>
  <action>
    Fix 5 sub-issues identified in the onboarding UAT debug session:

    1. **WelcomePage.swift** — Replace `DictusLogo(height: 100)` with `BrandWaveform(energyLevels: ..., maxHeight: 100)`. Create a gentle idle animation using a Timer that generates random low-energy values (0.1-0.4 range) to make the waveform breathe on the welcome screen. Import BrandWaveform if not already imported.

    2. **Button contrast on 3 files** — In KeyboardSetupPage.swift (line ~70), TestRecordingPage.swift (lines ~118 and ~135), find every `.foregroundStyle(.primary)` on buttons with colored backgrounds (`.dictusAccent` or `.dictusSuccess`) and replace with `.foregroundColor(.white)`. WelcomePage.swift line 42 already uses `.foregroundColor(.white)` — verify it is correct and leave it.

    3. **OnboardingView.swift** — Change `@State private var currentPage: Int = 0` to `@SceneStorage("onboarding_currentPage") private var currentPage: Int = 0`. This persists the page index across scene phase changes (backgrounding/foregrounding), preventing the reset-to-step-1 bug when returning from iOS Settings.

    4. **BrandWaveform.swift (DictusApp copy)** — In the `colorForBar()` method (around line 91-94), replace hardcoded `Color.white.opacity(...)` for outer bars with an adaptive color: use `@Environment(\.colorScheme) var colorScheme` and return `Color.gray.opacity(...)` in light mode, `Color.white.opacity(...)` in dark mode. The center bars (blue gradient) should remain unchanged.

    5. **DictusLogo.swift** — Same fix as BrandWaveform: replace `Color.white.opacity(opacities[index])` (line ~42) with adaptive color using `@Environment(\.colorScheme)`. Gray in light mode, white in dark mode.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild -project Dictus.xcodeproj -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WelcomePage shows BrandWaveform with gentle idle animation, not static DictusLogo
    - All onboarding buttons have white text on colored backgrounds
    - @SceneStorage persists currentPage across backgrounding
    - BrandWaveform bars are gray in light mode, white in dark mode
    - DictusLogo bars are gray in light mode, white in dark mode
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply glass styling to Models and Settings tabs</name>
  <files>
    DictusApp/Views/ModelManagerView.swift,
    DictusApp/Views/SettingsView.swift
  </files>
  <action>
    Apply .dictusGlass() to Models and Settings views to match the Home tab's glass card styling. Follow the pattern established in HomeView (ScrollView + VStack with .dictusGlass() on card containers).

    **ModelManagerView.swift:**
    - Change the List to a ScrollView + LazyVStack (or VStack) pattern similar to HomeView
    - Wrap each model row in a container with `.padding()` then `.dictusGlass()`
    - Use `.listRowBackground(Color.clear)` if keeping List, or switch to ScrollView entirely
    - Preserve all existing functionality (download, select, delete)

    **SettingsView.swift:**
    - For Settings, keep the native List with grouped style (it provides standard iOS settings UX) BUT apply glass to section backgrounds
    - Use `.listRowBackground(Color.clear)` on rows and wrap each Section content in a VStack with `.dictusGlass()`
    - Alternative approach if the above is too complex: keep native List styling as-is if the user's feedback said "would be better" (cosmetic, not blocking). In that case, just add `.scrollContentBackground(.hidden)` to the List and `.dictusGlass()` to each Section's header area.
    - Preserve all toggles, pickers, navigation links, and App Group persistence
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild -project Dictus.xcodeproj -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Models tab shows glass-styled cards for each model row
    - Settings tab has glass treatment on sections
    - All existing functionality preserved (toggles, downloads, navigation)
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- xcodebuild DictusApp succeeds
- Grep confirms no `DictusLogo` in WelcomePage.swift
- Grep confirms no `.foregroundStyle(.primary)` on colored buttons in onboarding files
- Grep confirms `@SceneStorage` in OnboardingView.swift
- Grep confirms `colorScheme` in BrandWaveform.swift (DictusApp copy)
- Grep confirms `.dictusGlass()` in ModelManagerView.swift and SettingsView.swift
</verification>

<success_criteria>
All 7 sub-issues from UAT Tests 3 and 6 are resolved. Onboarding works correctly in both light and dark mode with persistent state. All three app tabs have consistent glass styling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-app-onboarding-and-polish/04-04-SUMMARY.md`
</output>
