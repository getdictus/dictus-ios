---
phase: 15.2-cleaning-and-fix-github-issues
plan: 03
type: execute
wave: 2
depends_on: ["15.2-01", "15.2-02"]
files_modified: []
autonomous: true
requirements: ["#37", "#42", "#41"]
must_haves:
  truths:
    - "Issues #7, #8 are closed as resolved (Phases 11/14)"
    - "Issues #9 is closed as duplicate of #23"
    - "Issues #16, #21, #36 are closed referencing #37 as parent"
    - "Issues #37, #42, #41 are closed with fix references"
    - "Deferred issues (#17, #22, #23, #29, #31, #44) remain open"
  artifacts: []
  key_links: []
---

<objective>
Triage GitHub issues: close resolved/duplicate issues, close fixed bugs with references, and verify deferred issues remain open.

Purpose: Clean up the issue tracker so it accurately reflects project state before TestFlight beta. All bug fixes from Plans 01 and 02 are done -- this plan records the closures on GitHub.

Output: Updated GitHub issue states
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-cleaning-and-fix-github-issues/15.2-CONTEXT.md
@.planning/phases/15.2-cleaning-and-fix-github-issues/15.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close resolved and duplicate GitHub issues</name>
  <files></files>
  <read_first>
    - .planning/phases/15.2-cleaning-and-fix-github-issues/15.2-RESEARCH.md (Issue Triage Plan section -- exact close actions and references)
    - .planning/phases/15.2-cleaning-and-fix-github-issues/15.2-CONTEXT.md (locked decisions on issue consolidation)
  </read_first>
  <action>
Use `gh` CLI to close issues and add comments. Run these commands in the dictus repo:

**Close resolved issues (addressed in prior phases):**

```bash
gh issue close 7 --comment "Resolved in Phase 11 (Logging Foundation). Structured logging with PersistentLog, LogEvent enum, and export functionality implemented."
gh issue close 8 --comment "Resolved in Phase 14 (Model Pipeline). CoreML pre-compilation runs automatically after model download with visible progress indicator."
```

**Close duplicate issues:**

```bash
gh issue close 9 --comment "Duplicate of #23. Auto-return to source app is deferred to post-beta research."
```

**Close issues consolidated under #37 (cold start cluster):**

```bash
gh issue close 16 --comment "Consolidated under #37. Same root cause: cold start transitions between DictusApp and keyboard extension. Fix applied in Phase 15.2."
gh issue close 21 --comment "Consolidated under #37. Same root cause: cold start overlay instability during URL scheme app transitions. Fix applied in Phase 15.2."
gh issue close 36 --comment "Consolidated under #37. Waveform invisible after suspension -- initial fix in commit b94a5ad (onChange fallback), residual rendering fix in Phase 15.2 (renderTick forced redraw)."
```

**Close bugs fixed in Phase 15.2:**

```bash
gh issue close 37 --comment "Fixed in Phase 15.2: (1) Residual waveform rendering fixed with renderTick forced-redraw mechanism in BrandWaveform + objectWillChange.send() in KeyboardState. (2) Original 3 fixes already applied (background handler guard, cleanupStaleActivities race fix, coldStartActive cleanup)."
gh issue close 42 --comment "Fixed in Phase 15.2: LiveActivityManager refactored with strict state machine (LiveActivityPhase enum). All transitions validated against allowed source states. Auto-dismiss tasks verify phase after sleep to prevent firing during active recordings."
gh issue close 41 --comment "Fixed in Phase 15.2: lastResult, bufferEnergy, bufferSeconds cleared at the start of startDictation() before any guards. All 5 entry paths (in-app mic, keyboard warm, keyboard cold, DI tap, lockscreen) now clear previous transcription state."
```

**Verify deferred issues remain open:**

```bash
gh issue list --state open
```

Expected open issues: #17 (developer account), #22 (test suite), #23 (auto-return), #29 (key spacing), #31 (settings shortcuts), #44 (keyboard improvements). If any of these were accidentally closed, reopen them.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && gh issue list --state closed --limit 20 --json number,title,stateReason 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue view 7 --json state` shows state "CLOSED"
    - `gh issue view 8 --json state` shows state "CLOSED"
    - `gh issue view 9 --json state` shows state "CLOSED"
    - `gh issue view 16 --json state` shows state "CLOSED"
    - `gh issue view 21 --json state` shows state "CLOSED"
    - `gh issue view 36 --json state` shows state "CLOSED"
    - `gh issue view 37 --json state` shows state "CLOSED"
    - `gh issue view 42 --json state` shows state "CLOSED"
    - `gh issue view 41 --json state` shows state "CLOSED"
    - `gh issue view 17 --json state` shows state "OPEN"
    - `gh issue view 23 --json state` shows state "OPEN"
    - `gh issue view 29 --json state` shows state "OPEN"
  </acceptance_criteria>
  <done>9 issues closed (3 resolved, 1 duplicate, 3 consolidated, 3 fixed). 6 deferred issues confirmed open. GitHub issue tracker accurately reflects project state.</done>
</task>

</tasks>

<verification>
- `gh issue list --state open` shows only deferred issues (#17, #22, #23, #29, #31, #44)
- `gh issue list --state closed --limit 20` includes #7, #8, #9, #16, #21, #36, #37, #41, #42
- Each closed issue has a comment explaining the resolution
</verification>

<success_criteria>
1. All 9 target issues are closed with descriptive comments
2. Deferred issues remain open
3. No issues accidentally closed or reopened
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-cleaning-and-fix-github-issues/15.2-03-SUMMARY.md`
</output>
