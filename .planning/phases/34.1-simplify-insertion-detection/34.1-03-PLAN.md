---
phase: 34.1-simplify-insertion-detection
plan: 03
type: execute
wave: 3
depends_on: [34.1-01, 34.1-02]
files_modified:
  - .planning/phases/34.1-simplify-insertion-detection/device-verification.md
autonomous: false
requirements: [STAB-01]
gap_closure: true

must_haves:
  truths:
    - "≥15 real-device insertions in Notes/Messages/Safari with zero false-positive failure UX (no red banner shown to user)"
    - "At least one long-document insertion (≥500 chars pre-cursor) completes without triggering silentDrop or duplicate insertion"
    - "At least one empty-field insertion (cursor in fresh text field) logs as emptyFieldSuccess in probe"
    - "No duplicate text appears in any host app field across all verification runs"
    - "Probe logs remain privacy-safe — zero raw transcription text detected by grep audit"
  artifacts:
    - path: ".planning/phases/34.1-simplify-insertion-detection/device-verification.md"
      provides: "run-by-run verification log with probe outcomes, host apps, field states"
      contains: "emptyFieldSuccess|windowedSuccess|keyboardInsertProbe"
  key_links: []
---

<objective>
Validate Plan 34.1-01 + 34.1-02 on real iPhone with ≥15 dictations spanning short/medium/long fields in multiple host apps. Confirm zero false-positive failure UX, zero duplicate insertions, and classifier outcomes match expectations.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
@/Users/pierreviviere/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/34.1-simplify-insertion-detection/34.1-CONTEXT.md
@.planning/phases/34.1-simplify-insertion-detection/34.1-01-SUMMARY.md
@.planning/phases/34.1-simplify-insertion-detection/34.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold device-verification.md with run matrix</name>
  <files>.planning/phases/34.1-simplify-insertion-detection/device-verification.md</files>
  <action>
Create `.planning/phases/34.1-simplify-insertion-detection/device-verification.md` with the following structure:

```markdown
---
phase: 34.1
type: device-verification
created: 2026-04-16
device: TBD (fill with iPhone model + iOS version)
build: TBD (fill with CFBundleVersion)
---

# Phase 34.1 Device Verification — Simplified Insertion Detection

## Protocol

15-run sanity verification. Each run must:
1. Dictate via Dictus keyboard in a host app.
2. Confirm inserted text appears exactly once in the host field (no duplicates).
3. Confirm no red banner appears in the Dictus toolbar.
4. Export log and record the probe line.

Mix of scenarios:
- 5 runs in a short Notes field (≤100 chars pre-cursor).
- 5 runs in a long Notes field (≥500 chars pre-cursor — build one up across runs 1-5 if needed, then continue in same field).
- 3 runs in Messages (new or existing thread).
- 2 runs in Safari address bar or web form.

Include at least:
- One empty-field insertion (cursor in brand new text field).
- One cold-start insertion (background Dictus before dictating).
- One chained dictation (two back-to-back in the same field without leaving the app).

## Results Log

| # | Host app  | Scenario | transcriptionCount | beforeCount | afterCount | Classifier outcome | Duplicate? | Banner? |
|---|-----------|----------|--------------------|-------------|-----------|--------------------|-----------|---------|
| 1 |           |          |                    |             |           |                    |           |         |
| ...

## Privacy Audit

After runs complete, export full log and run:

```
grep -iE "message=|text=|transcription=|result=|content=" dictus-logs-34.1-verify.txt
```

Also grep for 2-3 distinctive words from the runs (e.g. a name, an unusual French word dictated). Both must return ZERO hits from probe/retry/failed lines.

## Pass Criteria

- [ ] ≥15 runs recorded
- [ ] Zero entries in "Duplicate?" column are "yes"
- [ ] Zero entries in "Banner?" column are "yes"
- [ ] At least one row has outcome = emptyFieldSuccess
- [ ] At least one row has beforeCount ≥ 500 with outcome = windowedSuccess (proves truncation no longer triggers false-positive)
- [ ] Privacy audit passes (zero raw text in probe lines)

## Observations

(Populate after runs.)
```
  </action>
  <verify>
    <automated>test -f /Users/pierreviviere/dev/dictus/.planning/phases/34.1-simplify-insertion-detection/device-verification.md &amp;&amp; grep -c "^|" /Users/pierreviviere/dev/dictus/.planning/phases/34.1-simplify-insertion-detection/device-verification.md</automated>
  </verify>
  <acceptance_criteria>
    - device-verification.md exists with the 4 required sections (Protocol, Results Log, Privacy Audit, Pass Criteria)
    - Template table row exists with all expected columns
  </acceptance_criteria>
  <done>Scaffold ready for user to fill in during device testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute 15-run device verification and populate results log</name>
  <what-built>Classifier fixed (34.1-01), helper simplified (34.1-02). User needs to validate on iPhone that the original regressions (3 false-positive categories, duplicate insertions, truncated banner) are all resolved.</what-built>
  <how-to-verify>
1. Install the build on iPhone. Note device + iOS version + build number at top of device-verification.md.
2. Run 15 dictations per the Protocol section, mixing host apps and field states.
3. After each dictation, visually confirm: (a) text appears exactly once, (b) no red banner, (c) no error haptic.
4. Export logs from DictusApp after every 5 runs (or all at end).
5. Parse probe lines and populate the Results Log table. Helper can parse the log for you if needed — just share the file path.
6. Run the Privacy Audit greps.
7. Fill the Observations section with anything noteworthy (unusual classifier outcomes, new edge cases, performance observations).
  </how-to-verify>
  <acceptance_criteria>
    - User reports: all 15+ runs had exactly one copy of dictated text in the host field (no duplicates)
    - User reports: no red banner shown during any run
    - User reports: no failure haptic during any run
    - Privacy audit returns zero raw text hits in probe/retry/failed log lines
    - At least one run exercises the long-document scenario (beforeCount ≥ 500) and classifies as success-family (not silentDrop)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all acceptance criteria pass. If any run showed a regression or unexpected outcome, share the probe log line so we can triage.</resume-signal>
  <done>User confirms Phase 34.1 corrected the regressions with zero new issues on real device.</done>
</task>

</tasks>

<verification>
No automated verification — this plan is entirely human-in-the-loop real-device testing.
</verification>

<success_criteria>
- device-verification.md populated with ≥15 runs
- All pass criteria check boxes ticked
- User approves the checkpoint
</success_criteria>

<output>
Create `.planning/phases/34.1-simplify-insertion-detection/34.1-03-SUMMARY.md` after user approval summarizing: device + build used, number of runs by host app, classifier outcome distribution, privacy audit results, any observations.
</output>
