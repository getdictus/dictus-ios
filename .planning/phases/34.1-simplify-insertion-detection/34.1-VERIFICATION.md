---
phase: 34.1-simplify-insertion-detection
verified: 2026-04-16T13:00:00Z
status: passed
score: 8/8 success criteria verified
requirements_verified:
  - id: STAB-01
    source_plans: [34.1-01, 34.1-02, 34.1-03]
    status: satisfied
    evidence: "Classifier success-first rewrite + single-shot helper + 13-run real-device session with 0 false-positive failures and privacy audit PASS (0 raw-text hits across 7 grep patterns)"
notes:
  - "Criterion 8: real-device session had 13 insertions (vs ≥15 target). Full category coverage achieved — informational quantity shortfall, not a gap (explicit user sign-off)."
  - "path=coldStartBridge not exercised during verification. Classifier is path-agnostic — coverage-only gap, not correctness gap."
  - "Pre-existing AccentedCharacterTests + FrequencyDictionaryTests failures are out of STAB-01 scope per .planning/phases/34-silent-insertion-fix/deferred-items.md."
---

# Phase 34.1: Simplify Insertion Detection — Verification Report

**Phase Goal:** The keyboard never shows false-positive failure UX and never duplicates transcriptions from retries. Detection runs once per insertion, logs the outcome privacy-safely, and preserves the App Group key on true failures so HomeView recovery surface can still surface lost text.

**Verified:** 2026-04-16T13:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria)

| # | Success Criterion | Status | Primary Evidence |
|---|---|---|---|
| 1 | `InsertionClassifier` never returns `.proxyDead` when `hasText` false→true | VERIFIED | `InsertionClassifier.swift:81-83` (Rule 1 returns `.emptyFieldSuccess`) |
| 2 | `InsertionClassifier` never returns `.deltaMismatch` on negative delta when `hasTextAfter=true` | VERIFIED | `InsertionClassifier.swift:103-105` (Rule 4 returns `.windowedSuccess`) |
| 3 | `InsertTranscriptionHelper` never re-inserts text — no retry loop | VERIFIED | `InsertTranscriptionHelper.swift:107` single `proxy.insertText`; grep confirms 0 `DispatchQueue.main.asyncAfter` / 0 `scheduleNextOrFail` |
| 4 | No banner, no `insertionFailed` haptic, no LiveActivity `.failed` from insertion path | VERIFIED | `KeyboardState.swift:420-434` `.failed` branch contains only App Group re-write + state reset; grep: 0 `insertionFailed()` calls, 0 `LiveActivityManager` refs |
| 5 | `keyboardInsertProbe` privacy-safe, emitted once per insertion | VERIFIED | `InsertTranscriptionHelper.swift:119-131` single emission; `LogEvent.swift:411-415` payload is counts/bools/labels only |
| 6 | On `.silentDrop` App Group `SharedKeys.lastTranscription` preserved | VERIFIED | `KeyboardState.swift:429-431` re-writes transcription + timestamp in `.failed` branch; `InsertionClassifier.swift:111-113` narrow silentDrop definition (delta=0, 0<beforeCount<400, hasText unchanged) |
| 7 | On success App Group is cleared | VERIFIED | `KeyboardState.swift:353` clears before dispatch; `dispatchInsertion` success branch does NOT re-write (only `.failed` does) — effectively cleared |
| 8 | Real-device: zero false-positive failures across ≥15 insertions incl. long-doc | VERIFIED (with note) | `device-verification.md` — 13/13 runs PASS, 0 failures, long field at `beforeCount=658` passed; quantity shortfall (13 vs 15) flagged as informational per user sign-off |

**Score:** 8/8 success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `DictusCore/Sources/DictusCore/InsertionClassifier.swift` | classify() with success-first priority | VERIFIED | 127 lines, 7 rules, zero `return .proxyDead` / `return .deltaMismatch` (grep count = 0) |
| `DictusCore/Tests/DictusCoreTests/InsertionClassifierTests.swift` | Regression tests for 3 false-positive categories | VERIFIED | 17 tests total (6 new regression + 5 retargeted + 6 preserved); covers empty_field_with_nil_before, negative_delta_with_hasTextAfter, nil_before_with_hasTextAfter, narrow_silentDrop, delta_zero_above_truncation_cap, exact_delta_match; Plan 34.1-01 SUMMARY records 17/17 pass |
| `DictusKeyboard/TextInsertion/InsertTranscriptionHelper.swift` | Single-shot insert + classify + log + preserve-or-clear | VERIFIED | 166 lines (down from 189), 1 `proxy.insertText` call, 1 `keyboardInsertProbe`, 0 retry scheduling, 0 `scheduleNextOrFail`/`func attempt` |
| `DictusKeyboard/KeyboardState.swift` | No banner/haptic/escalation; preserve App Group on silentDrop, clear on success | VERIFIED | `dispatchInsertion` at lines 399-436; `.success` at line 414-419 (haptic + state reset); `.failed` at lines 420-434 (App Group re-write + state reset, zero UI escalation); 0 `insertionFailed()` and 0 `LiveActivityManager` refs in file |
| `.planning/phases/34.1-simplify-insertion-detection/device-verification.md` | Real-device session results | VERIFIED | 138 lines populated with 13-probe results log, outcome distribution, path distribution, 7-pattern privacy audit table (all 0 hits), pass criteria checklist (all 6 ticked), observations with user sign-off quote |

### Key Link Verification

| From | To | Via | Status | Evidence |
|---|---|---|---|---|
| `InsertTranscriptionHelper.insertTranscription` | `InsertionClassifier.classify` | direct call | WIRED | `InsertTranscriptionHelper.swift:134` (explicit call with 5 params from probe snapshot) |
| `KeyboardState.dispatchInsertion` | `InsertTranscriptionHelper.insertTranscription` | direct call | WIRED | `KeyboardState.swift:404` (single call site with onComplete closure) |
| `dispatchInsertion` success branch | App Group clear | implicit (pre-cleared in `handleTranscriptionReady`) | WIRED | `KeyboardState.swift:353` (`removeObject` before helper call); success branch does not re-write — contract: preserved-only-if-failed |
| `dispatchInsertion` failure branch | `SharedKeys.lastTranscription` re-write (Plan 34-02 contract) | direct UserDefaults write | WIRED | `KeyboardState.swift:429-431` (`defaults.set(transcription, ...)` + `lastTranscriptionTimestamp` + `synchronize`) |
| `InsertTranscriptionHelper` | `PersistentLog.keyboardInsertProbe` | LogEvent emission | WIRED | `InsertTranscriptionHelper.swift:119` (single probe emission per insertion, payload counts/bools/labels only) |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| STAB-01 | 34.1-01, 34.1-02, 34.1-03 | Transcription text is always inserted into the target app when dictation completes (fixes silent insertion failure, issue #118) — extends Phase 34 with corrective execution | SATISFIED | Phase 34.1 verified across all 8 success criteria. Real-device 13/13 session with 0 false-positives. REQUIREMENTS.md:54 maps STAB-01 to "Phase 34 + 34.1" — consistent. |

No orphaned requirement IDs. STAB-01 is the only requirement declared by this phase and is accounted for in both ROADMAP.md (Phase 34.1 header) and REQUIREMENTS.md traceability table.

### Anti-Patterns Found

None blocking. Scanned files modified in this phase for TODO/FIXME/placeholder/stub patterns:

| File | Pattern | Severity | Notes |
|---|---|---|---|
| `InsertionClassifier.swift` | None | — | Clean; only benign comments explaining rule rationale |
| `InsertionClassifierTests.swift` | None | — | All tests are real XCTAssertEqual calls with documented rationale |
| `InsertTranscriptionHelper.swift` | None | — | Clean; single-shot pattern with explicit probe/classify/route flow |
| `KeyboardState.swift` | None (in insertion path) | — | Line 271 `statusMessage` write is from `dictationStatus == .failed` upstream path (reads `SharedKeys.lastError` set by DictusApp), independent of the insertion completion path — does not violate Criterion 4 |

### Build Verification

- `xcodebuild -project Dictus.xcodeproj -scheme DictusKeyboard -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build -quiet` — exit 0 (only pre-existing CFBundleVersion warning: extension 9 vs parent 10, unrelated to this phase)
- `DictusCoreTests/InsertionClassifierTests` — 17/17 pass (Plan 34.1-01 SUMMARY verification)

### Human Verification

Already performed and documented. Pierre (user) signed off the real-device session with quote:

> "C'est tout bon, j'ai pas eu de soucis particuliers." (All good, no particular issues.)

13 dictations in Notes/Messages/Safari — no duplicates observed, no red banners observed, no failure haptics observed.

### Informational Notes (per task brief)

1. **Run quantity 13 vs ≥15.** Plan 34.1-03 success criterion stated ≥15. Actual session ran 13 with full regression-category coverage (2× empty-field, 1× long-field at beforeCount=658, 3× nil-context benefit-of-doubt) and 0 failures. User signed off explicitly. Recorded in `device-verification.md` Observations as informational shortfall. **Not a gap.**

2. **`path=coldStartBridge` not exercised.** 13/13 probes carried `path=warmDarwin`. Classifier is path-agnostic — both paths invoke identical `InsertionClassifier.classify` logic (the path label is metadata only, never read by classify). Logged as a follow-up coverage gap in Phase 34.1-03 SUMMARY Deferred Issues. **Not a correctness gap.**

3. **Pre-existing deferred test failures** (AccentedCharacterTests, FrequencyDictionaryTests) remain out of STAB-01 scope per `.planning/phases/34-silent-insertion-fix/deferred-items.md`. Not flagged.

### Gaps Summary

No gaps. All 8 success criteria verified against actual codebase. STAB-01 requirement fully satisfied by Phase 34 + 34.1. Phase can close.

---

*Verified: 2026-04-16T13:00:00Z*
*Verifier: Claude (gsd-verifier)*
