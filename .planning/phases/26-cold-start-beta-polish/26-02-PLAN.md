---
phase: 26-cold-start-beta-polish
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - DictusApp/Views/SwipeBackOverlayView.swift
  - DictusApp/Localizable.xcstrings
autonomous: false
requirements: [COLD-03, BETA-01]
must_haves:
  truths:
    - "User sees a Wispr Flow-style overlay with large iPhone mockup showing recording state"
    - "User sees animated swipe gesture on the home bar area of the mockup"
    - "User reads empathetic explanation text in their device language (FR or EN)"
    - "Overlay text references returning to their previous app, NOT 'the keyboard'"
    - "Critical beta bugs are triaged with fixes applied or GitHub issues filed"
  artifacts:
    - path: "DictusApp/Views/SwipeBackOverlayView.swift"
      provides: "Wispr Flow-style swipe-back overlay with iPhone mockup, waveform, gesture animation"
      min_lines: 100
    - path: "DictusApp/Localizable.xcstrings"
      provides: "French translations for overlay text strings"
      contains: "Dictation in progress"
  key_links:
    - from: "DictusApp/Views/SwipeBackOverlayView.swift"
      to: "DictusApp/Views/MainTabView.swift"
      via: "SwipeBackOverlayView() called at line 42"
      pattern: "SwipeBackOverlayView"
    - from: "DictusApp/Views/SwipeBackOverlayView.swift"
      to: "DictusApp/Localizable.xcstrings"
      via: "SwiftUI Text() auto-lookup in String Catalog"
      pattern: "Text\\("
---

<objective>
Redesign the swipe-back overlay with Wispr Flow-style visual gesture teaching and triage any critical beta bugs.

Purpose: COLD-03 requires a redesigned overlay that TEACHES the swipe-back gesture visually (a real user didn't know the gesture existed). BETA-01 requires triaging reported bugs.
Output: Redesigned SwipeBackOverlayView.swift with iPhone mockup + animated gesture + localized empathetic text. Beta bugs triaged.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-cold-start-beta-polish/26-CONTEXT.md
@.planning/phases/26-cold-start-beta-polish/26-RESEARCH.md
@.planning/phases/26-cold-start-beta-polish/26-01-SUMMARY.md

<interfaces>
<!-- Current overlay integration point in MainTabView.swift -->

From DictusApp/Views/MainTabView.swift (lines 36-43):
```swift
var body: some View {
    ZStack {
        if isColdStartMode {
            SwipeBackOverlayView()  // <-- This is the integration point. No props needed.
        } else {
            TabView(selection: $selectedTab) { ... }
        }
    }
}
```

From DictusApp/Views/SwipeBackOverlayView.swift (current, to be replaced):
```swift
struct SwipeBackOverlayView: View {
    // Current: 147 lines, simple layout with 120x260 iPhone outline
    // No props, no @EnvironmentObject, no external dependencies
}
```

Brand colors (from CLAUDE.md):
```swift
Color(hex: 0x0D2040) // background gradient start
Color(hex: 0x071020) // background gradient end
Color.dictusAccent    // #3D7EFF
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign SwipeBackOverlayView with Wispr Flow-style layout</name>
  <read_first>
    - DictusApp/Views/SwipeBackOverlayView.swift (current implementation to replace)
    - DictusApp/Views/MainTabView.swift (integration point, lines 36-43 -- must keep same struct name and no props)
    - .planning/phases/26-cold-start-beta-polish/26-RESEARCH.md (design spec, code examples, anti-patterns)
    - assets/brand/dictus-brand-kit.html (exact brand colors for overlay styling)
  </read_first>
  <files>DictusApp/Views/SwipeBackOverlayView.swift</files>
  <action>
    Replace the entire content of SwipeBackOverlayView.swift with a Wispr Flow-style redesign. The struct name MUST remain `SwipeBackOverlayView` with no parameters (MainTabView calls `SwipeBackOverlayView()` with no arguments).

    **Layout (2-zone, top to bottom):**

    1. **Title** at top: `Text("Dictation in progress")` with `.font(.title2.weight(.semibold))`, white color
    2. **Spacer**
    3. **IPhoneMockupView** (new private struct) in center -- 180x390pt:
       - Device outline: `RoundedRectangle(cornerRadius: 36, style: .continuous)` with `.stroke(Color.white.opacity(0.3), lineWidth: 2.5)`
       - Dynamic Island capsule: `RoundedRectangle(cornerRadius: 10)`, `.fill(Color.white.opacity(0.25))`, 50x14pt, padded 18pt from top
       - Inner content (centered): 5 simplified waveform bars (`RoundedRectangle(cornerRadius: 2)`, `.fill(Color.dictusAccent)`, width 4pt each, heights animate between random 6-20 and 12-40) + "Listening..." label below in `.caption2` white 70% opacity
       - Swipe gesture animation at bottom: animated accent circle (28x28pt) sliding from x:-30 to x:+50, with 2 chevron.right trail icons fading behind it. Animation: `.easeInOut(duration: 1.2).repeatForever(autoreverses: false)`
       - Home indicator bar: `RoundedRectangle(cornerRadius: 3)`, white 40% opacity, 60x5pt, padded 14pt from bottom
    4. **Empathetic explanation text** below mockup:
       - `Text("We'd love to skip this step, but iOS requires switching apps to activate the microphone.")`
       - `.font(.subheadline)`, `.foregroundColor(.white.opacity(0.7))`, `.multilineTextAlignment(.center)`, `.padding(.horizontal, 32)`
    5. **Spacer**
    6. **Bottom instruction** pinned at bottom:
       - `Text("Swipe right at the bottom of your screen")`
       - `.font(.callout.weight(.medium))`, `.foregroundColor(.dictusAccent)`
       - Add padding bottom 40pt for safe area clearance

    **Background:** Same brand gradient as current: `LinearGradient(colors: [Color(hex: 0x0D2040), Color(hex: 0x071020)], startPoint: .topLeading, endPoint: .bottomTrailing).ignoresSafeArea()`

    **Animation state:** Single `@State private var isAnimating = false` toggled in `onAppear` with `withAnimation(.easeInOut(duration: 1.2).repeatForever(autoreverses: false)) { isAnimating = true }`. Both waveform bars and swipe circle use this state.

    **Waveform bar animation:** Use a SEPARATE animation modifier on the waveform HStack: `.animation(.easeInOut(duration: 0.6).repeatForever(autoreverses: true), value: isAnimating)` -- this gives the bars a pulsing effect while the swipe circle has a sliding effect.

    **CRITICAL: Do NOT use `CGFloat.random(in:)` inside the view body** -- SwiftUI recalculates random values on every frame, causing jitter. Instead, define fixed bar heights as constants:
    ```swift
    private let barHeightsIdle: [CGFloat] = [8, 14, 6, 12, 10]
    private let barHeightsActive: [CGFloat] = [28, 38, 16, 34, 22]
    ```

    **Keep `#Preview` block** at the bottom for Xcode Preview testing.

    **Do NOT:**
    - Add any recording/audio logic (overlay is purely visual, recording happens in DictationCoordinator)
    - Use the real WaveformCanvasView inside the mockup (too heavy for a 180pt mockup)
    - Import any new frameworks beyond SwiftUI
    - Add parameters to SwipeBackOverlayView struct
  </action>
  <verify>
    <automated>grep -c "IPhoneMockupView" DictusApp/Views/SwipeBackOverlayView.swift | grep -q "[1-9]" && echo "PASS: mockup struct exists" || echo "FAIL"; grep -q "Dictation in progress" DictusApp/Views/SwipeBackOverlayView.swift && echo "PASS: title text" || echo "FAIL"; grep -q "switching apps to activate the microphone" DictusApp/Views/SwipeBackOverlayView.swift && echo "PASS: empathetic text" || echo "FAIL"; grep -q "Swipe right at the bottom" DictusApp/Views/SwipeBackOverlayView.swift && echo "PASS: bottom instruction" || echo "FAIL"; grep -q "struct SwipeBackOverlayView: View" DictusApp/Views/SwipeBackOverlayView.swift && echo "PASS: struct name preserved" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `struct SwipeBackOverlayView: View` with no init parameters
    - File contains `private struct IPhoneMockupView: View`
    - File contains `Text("Dictation in progress")`
    - File contains `Text("We'd love to skip this step, but iOS requires switching apps to activate the microphone.")`
    - File contains `Text("Swipe right at the bottom of your screen")`
    - File does NOT contain "Swipe back to the keyboard" (old text removed)
    - File contains `.repeatForever(autoreverses: false)` for swipe animation
    - File contains `frame(width: 180, height: 390)` for mockup size
    - File contains `#Preview` block
    - No import beyond SwiftUI
  </acceptance_criteria>
  <done>SwipeBackOverlayView.swift fully redesigned with Wispr Flow-style 2-zone layout: iPhone mockup with waveform + swipe animation in center, empathetic localized text, bottom instruction. Old "Swipe back to the keyboard" text is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add French translations to String Catalog for overlay text</name>
  <read_first>
    - DictusApp/Localizable.xcstrings (current String Catalog -- check existing keys and format)
    - DictusApp/Views/SwipeBackOverlayView.swift (new English source strings from Task 1)
  </read_first>
  <files>DictusApp/Localizable.xcstrings</files>
  <action>
    Open `DictusApp/Localizable.xcstrings` and add French translations for the 3 new English source strings used in SwipeBackOverlayView.

    **Strings to add (key = English source, value = French translation):**

    1. Key: `"Dictation in progress"`
       FR: `"Dictee en cours"` (with proper accent: `"Dict\u00e9e en cours"`)

    2. Key: `"We'd love to skip this step, but iOS requires switching apps to activate the microphone."`
       FR: `"On aimerait \u00e9viter cette \u00e9tape, mais iOS exige de changer d\u2019app pour activer le micro."`

    3. Key: `"Swipe right at the bottom of your screen"`
       FR: `"Glissez vers la droite en bas de votre \u00e9cran"`

    **Also remove old overlay strings if they exist:**
    - Remove key `"Swipe back to the keyboard"` (old primary text)
    - Remove key `"Swipe right on the bottom of your iPhone"` (old secondary text)

    **Format:** Follow existing xcstrings JSON format. Each entry needs:
    ```json
    "Dictation in progress" : {
      "localizations" : {
        "fr" : {
          "stringUnit" : {
            "state" : "translated",
            "value" : "Dict\u00e9e en cours"
          }
        }
      }
    }
    ```

    Use proper French accents throughout. Check existing entries in the String Catalog for accent convention consistency.
  </action>
  <verify>
    <automated>grep -c "Dictation in progress" DictusApp/Localizable.xcstrings | grep -q "[1-9]" && echo "PASS: title key exists" || echo "FAIL"; grep -c "switching apps to activate" DictusApp/Localizable.xcstrings | grep -q "[1-9]" && echo "PASS: empathetic text key exists" || echo "FAIL"; grep -c "Swipe right at the bottom" DictusApp/Localizable.xcstrings | grep -q "[1-9]" && echo "PASS: instruction key exists" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Localizable.xcstrings contains key "Dictation in progress" with French translation
    - Localizable.xcstrings contains key matching "switching apps to activate the microphone" with French translation
    - Localizable.xcstrings contains key "Swipe right at the bottom of your screen" with French translation
    - Old keys "Swipe back to the keyboard" and "Swipe right on the bottom of your iPhone" are removed (if they existed)
    - All French translations contain proper accented characters (not ASCII approximations)
  </acceptance_criteria>
  <done>String Catalog updated with 3 new FR translations for overlay. Old overlay strings removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify overlay design + triage beta bugs</name>
  <files>DictusApp/Views/SwipeBackOverlayView.swift, DictusApp/Localizable.xcstrings</files>
  <action>
    Human verifies the redesigned overlay on a physical device and triages beta feedback.

    **Overlay verification:**
    1. Open Xcode, build DictusApp on physical device
    2. Kill DictusApp completely (swipe up from app switcher)
    3. Open any third-party app (WhatsApp, Notes, Safari)
    4. Switch to Dictus keyboard, tap the mic button
    5. DictusApp should open and show the redesigned overlay
    6. Verify: iPhone mockup is visible with animated waveform bars
    7. Verify: Accent circle slides right on the home bar area of the mockup
    8. Verify: Empathetic text is shown in device language (FR or EN)
    9. Verify: Bottom instruction says "Swipe right at the bottom of your screen" (NOT "Swipe back to the keyboard")
    10. Swipe right from the bottom of the screen to return to the previous app
    11. Verify: Recording completed and transcription inserted

    **Localization check:**
    12. Change device language to English, repeat steps 2-9, verify English text
    13. Change back to French, verify French text with proper accents

    **Beta bug triage (BETA-01):**
    14. Check TestFlight feedback: App Store Connect -> App -> TestFlight -> Feedback
    15. Check GitHub issues: `gh issue list --label bug`
    16. For each reported bug: if user can't dictate or correct text -> critical (fix now), cosmetic or edge case -> file as GitHub issue for v1.5
    17. Report what was found: number of bugs, severity, action taken

    Type "approved" if overlay looks good and beta triage is complete, or describe issues.
  </action>
  <verify>
    <automated>grep -q "IPhoneMockupView" DictusApp/Views/SwipeBackOverlayView.swift && grep -q "Dictation in progress" DictusApp/Localizable.xcstrings && echo "PASS: overlay + translations present" || echo "FAIL"</automated>
  </verify>
  <done>Overlay verified on device with correct layout, animations, and localization. Beta bugs triaged -- critical fixed, non-critical filed as GitHub issues.</done>
</task>

</tasks>

<verification>
- SwipeBackOverlayView.swift contains Wispr Flow-style design with IPhoneMockupView
- No reference to "Swipe back to the keyboard" anywhere in overlay
- French translations exist in String Catalog
- Overlay renders correctly on physical device during cold start
- Beta bugs triaged (0 is a valid outcome if no bugs reported)
</verification>

<success_criteria>
- COLD-03: Overlay teaches the swipe-back gesture visually with iPhone mockup + animated swipe circle + empathetic localized text
- BETA-01: Beta feedback checked (TestFlight + GitHub), critical bugs fixed, non-critical bugs filed
</success_criteria>

<output>
After completion, create `.planning/phases/26-cold-start-beta-polish/26-02-SUMMARY.md`
</output>
