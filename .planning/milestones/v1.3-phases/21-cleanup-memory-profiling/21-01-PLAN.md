---
phase: 21-cleanup-memory-profiling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DictusKeyboard/KeyboardMetrics.swift
  - DictusKeyboard/LegacyCompat.swift
  - DictusKeyboard/Views/KeyButton.swift
  - DictusKeyboard/Views/KeyRow.swift
  - DictusKeyboard/Views/KeyboardView.swift
  - DictusKeyboard/Views/SpecialKeyButton.swift
  - DictusKeyboard/Views/AccentPopup.swift
  - Dictus.xcodeproj/project.pbxproj
autonomous: true
requirements: []

must_haves:
  truths:
    - "Old SwiftUI keyboard files are deleted from disk and project"
    - "LegacyCompat types are preserved in a permanent file for active consumers"
    - "Project compiles cleanly after all deletions"
    - "No ghost references remain in pbxproj"
  artifacts:
    - path: "DictusKeyboard/KeyboardMetrics.swift"
      provides: "DeviceClass, KeyMetrics, KeySound, KeyPopup types"
      contains: "enum DeviceClass"
    - path: "DictusKeyboard/Views/KeyButton.swift"
      provides: "MUST NOT EXIST (deleted)"
    - path: "DictusKeyboard/LegacyCompat.swift"
      provides: "MUST NOT EXIST (deleted)"
  key_links:
    - from: "DictusKeyboard/Views/EmojiPickerView.swift"
      to: "DictusKeyboard/KeyboardMetrics.swift"
      via: "KeyMetrics, KeyPopup type usage"
      pattern: "KeyMetrics\\."
    - from: "DictusKeyboard/DictusKeyboardBridge.swift"
      to: "DictusKeyboard/KeyboardMetrics.swift"
      via: "KeySound type usage"
      pattern: "KeySound\\."
---

<objective>
Delete all old SwiftUI keyboard code, extract reusable types from LegacyCompat into a permanent file, clean the Xcode project file of ghost references, and verify the project compiles.

Purpose: Remove technical debt from the Phase 18 keyboard rebuild. Old SwiftUI files are dead code that bloats the project and confuses navigation. LegacyCompat was always intended as temporary.
Output: Clean codebase with no old SwiftUI keyboard files, a permanent KeyboardMetrics.swift, and a project that compiles.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cleanup-memory-profiling/21-CONTEXT.md
@.planning/phases/21-cleanup-memory-profiling/21-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From DictusKeyboard/LegacyCompat.swift (source of types to extract):
```swift
enum DeviceClass {
    case compact    // iPhone SE
    case standard   // iPhone 14/15/16
    case large      // iPhone Plus/Max
    static let current: DeviceClass
}

enum KeyMetrics {
    static let keyHeight: CGFloat
    static let rowSpacing: CGFloat
    static let keySpacing: CGFloat
    static let rowSidePadding: CGFloat
    static let keyCornerRadius: CGFloat
    static let letterKeyColor: Color
    static let pressedKeyColor: Color
}

enum KeySound {
    static let letter: SystemSoundID   // 1104
    static let delete: SystemSoundID   // 1155
    static let modifier: SystemSoundID // 1156
}

struct KeyPopup: View {
    let label: String
}
```

Consumers of these types (must keep compiling):
- DictusKeyboard/Views/EmojiPickerView.swift -- uses KeyMetrics, KeyPopup
- DictusKeyboard/Views/EmojiCategoryBar.swift -- uses KeyMetrics
- DictusKeyboard/DictusKeyboardBridge.swift -- uses KeySound

pbxproj IDs to REMOVE:
- PBXBuildFile: AA000031, AA000032, AA000033, AA000034, AA000070, CC000000
- PBXFileReference: AA100031, AA100032, AA100033, AA100034, AA100070, CC100000
- PBXGroup (Views children): AA100031, AA100032, AA100033, AA100034, AA100070
- PBXGroup (DictusKeyboard children): CC100000

pbxproj IDs to KEEP (Vendored UIKit keyboard):
- CC000009 / CC100009 -- KeyboardView.swift in Vendored/Views/ (GiellaKeyboardView)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LegacyCompat types into KeyboardMetrics.swift and delete old files</name>
  <files>
    DictusKeyboard/KeyboardMetrics.swift,
    DictusKeyboard/LegacyCompat.swift,
    DictusKeyboard/Views/KeyButton.swift,
    DictusKeyboard/Views/KeyRow.swift,
    DictusKeyboard/Views/KeyboardView.swift,
    DictusKeyboard/Views/SpecialKeyButton.swift,
    DictusKeyboard/Views/AccentPopup.swift
  </files>
  <read_first>
    DictusKeyboard/LegacyCompat.swift,
    DictusKeyboard/Views/EmojiPickerView.swift,
    DictusKeyboard/Views/EmojiCategoryBar.swift,
    DictusKeyboard/DictusKeyboardBridge.swift,
    DictusKeyboard/KeyboardRootView.swift
  </read_first>
  <action>
1. Create `DictusKeyboard/KeyboardMetrics.swift` with the following exact content (copy all 4 types from LegacyCompat.swift verbatim):
   - File header comment: `// DictusKeyboard/KeyboardMetrics.swift` + `// Keyboard layout constants and feedback types. Extracted from LegacyCompat.swift (Phase 21).` + `// Used by EmojiPickerView, EmojiCategoryBar, DictusKeyboardBridge.`
   - Imports: `import SwiftUI`, `import UIKit`, `import AudioToolbox` (NOT AVFoundation, NOT DictusCore -- those were only needed by LegacyCompat's dead placeholder)
   - Copy `enum DeviceClass` exactly as-is from LegacyCompat (lines 16-27)
   - Copy `enum KeyMetrics` exactly as-is from LegacyCompat (lines 32-78)
   - Copy `enum KeySound` exactly as-is from LegacyCompat (lines 84-88)
   - Copy `struct KeyPopup: View` exactly as-is from LegacyCompat (lines 93-102)
   - Do NOT copy the placeholder `struct KeyboardView` (lines 109-122) -- it is dead code

2. Delete the following 6 files from disk:
   - `DictusKeyboard/LegacyCompat.swift`
   - `DictusKeyboard/Views/KeyButton.swift`
   - `DictusKeyboard/Views/KeyRow.swift`
   - `DictusKeyboard/Views/KeyboardView.swift` (this is the SwiftUI one in Views/, NOT the UIKit one in Vendored/Views/)
   - `DictusKeyboard/Views/SpecialKeyButton.swift`
   - `DictusKeyboard/Views/AccentPopup.swift`

CRITICAL: Do NOT delete `DictusKeyboard/Vendored/Views/KeyboardView.swift` -- that is the active UIKit GiellaKeyboardView.

3. After creating KeyboardMetrics.swift, verify the consumers (EmojiPickerView, EmojiCategoryBar, DictusKeyboardBridge) do not need any import changes -- they should resolve the types from the same module since both old and new files are in DictusKeyboard target.
  </action>
  <verify>
    <automated>test ! -f DictusKeyboard/LegacyCompat.swift && test ! -f DictusKeyboard/Views/KeyButton.swift && test ! -f DictusKeyboard/Views/KeyRow.swift && test ! -f DictusKeyboard/Views/SpecialKeyButton.swift && test ! -f DictusKeyboard/Views/AccentPopup.swift && test -f DictusKeyboard/KeyboardMetrics.swift && test -f DictusKeyboard/Vendored/Views/KeyboardView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - DictusKeyboard/KeyboardMetrics.swift exists and contains `enum DeviceClass`, `enum KeyMetrics`, `enum KeySound`, `struct KeyPopup`
    - DictusKeyboard/KeyboardMetrics.swift does NOT contain `struct KeyboardView` or `import AVFoundation` or `import DictusCore`
    - DictusKeyboard/LegacyCompat.swift does NOT exist
    - DictusKeyboard/Views/KeyButton.swift does NOT exist
    - DictusKeyboard/Views/KeyRow.swift does NOT exist
    - DictusKeyboard/Views/KeyboardView.swift does NOT exist (but Vendored/Views/KeyboardView.swift DOES exist)
    - DictusKeyboard/Views/SpecialKeyButton.swift does NOT exist
    - DictusKeyboard/Views/AccentPopup.swift does NOT exist
  </acceptance_criteria>
  <done>6 old files deleted, KeyboardMetrics.swift created with all 4 reusable types, Vendored KeyboardView untouched</done>
</task>

<task type="auto">
  <name>Task 2: Clean pbxproj and add KeyboardMetrics.swift, then verify build</name>
  <files>
    Dictus.xcodeproj/project.pbxproj
  </files>
  <read_first>
    Dictus.xcodeproj/project.pbxproj,
    DictusKeyboard/KeyboardMetrics.swift
  </read_first>
  <action>
1. Edit `Dictus.xcodeproj/project.pbxproj` to remove ALL references to the 6 deleted files. Target lines by their unique IDs:

   **Remove from PBXBuildFile section (lines ~24-88):**
   - Line with `AA000031 /* KeyButton.swift in Sources */`
   - Line with `AA000032 /* SpecialKeyButton.swift in Sources */`
   - Line with `AA000033 /* KeyRow.swift in Sources */`
   - Line with `AA000034 /* KeyboardView.swift in Sources */`
   - Line with `AA000070 /* AccentPopup.swift in Sources */`
   - Line with `CC000000 /* LegacyCompat.swift in Sources */`

   **Remove from PBXFileReference section (lines ~154-219):**
   - Line with `AA100031 /* KeyButton.swift */`
   - Line with `AA100032 /* SpecialKeyButton.swift */`
   - Line with `AA100033 /* KeyRow.swift */`
   - Line with `AA100034 /* KeyboardView.swift */`
   - Line with `AA100070 /* AccentPopup.swift */`
   - Line with `CC100000 /* LegacyCompat.swift */`

   **Remove from PBXGroup children (Views group, lines ~348-356):**
   - Line with `AA100031 /* KeyButton.swift */,`
   - Line with `AA100032 /* SpecialKeyButton.swift */,`
   - Line with `AA100033 /* KeyRow.swift */,`
   - Line with `AA100034 /* KeyboardView.swift */,`
   - Line with `AA100070 /* AccentPopup.swift */,`

   **Remove from PBXGroup children (DictusKeyboard group, line ~311):**
   - Line with `CC100000 /* LegacyCompat.swift */,`

   **Remove from PBXSourcesBuildPhase (line ~735):**
   - Line with `CC000000 /* LegacyCompat.swift in Sources */,`
   - Note: AA000031-AA000034 and AA000070 are NOT in the SourcesBuildPhase (already removed in prior phase), so no action needed there.

   **CRITICAL: Do NOT remove CC000009 or CC100009** -- those are the Vendored KeyboardView.swift (GiellaKeyboardView).

2. Add KeyboardMetrics.swift to the project. Use ID pattern matching existing vendored files:
   - Pick a new unique ID pair, e.g. `CC000030` (PBXBuildFile) and `CC100030` (PBXFileReference)
   - Add PBXBuildFile entry: `CC000030 /* KeyboardMetrics.swift in Sources */ = {isa = PBXBuildFile; fileRef = CC100030 /* KeyboardMetrics.swift */; };`
   - Add PBXFileReference entry: `CC100030 /* KeyboardMetrics.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = KeyboardMetrics.swift; sourceTree = "<group>"; };`
   - Add to DictusKeyboard PBXGroup children (where CC100000 was): `CC100030 /* KeyboardMetrics.swift */,`
   - Add to PBXSourcesBuildPhase (where CC000000 was): `CC000030 /* KeyboardMetrics.swift in Sources */,`

3. Run build to verify compilation:
   `xcodebuild build -scheme DictusKeyboard -destination 'generic/platform=iOS' -quiet 2>&1 | tail -5`
   Expect: `** BUILD SUCCEEDED **`
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && grep -c 'AA100031\|AA100032\|AA100033\|AA100034\|AA100070\|CC100000\|AA000031\|AA000032\|AA000033\|AA000034\|AA000070\|CC000000' Dictus.xcodeproj/project.pbxproj | grep '^0$' && grep -c 'CC100030.*KeyboardMetrics' Dictus.xcodeproj/project.pbxproj | grep -v '^0$' && echo "PBXPROJ CLEAN" || echo "PBXPROJ DIRTY"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AA100031\|AA100032\|AA100033\|AA100034\|AA100070\|CC100000' Dictus.xcodeproj/project.pbxproj` returns 0
    - `grep -c 'AA000031\|AA000032\|AA000033\|AA000034\|AA000070\|CC000000' Dictus.xcodeproj/project.pbxproj` returns 0
    - `grep -c 'CC100030.*KeyboardMetrics' Dictus.xcodeproj/project.pbxproj` returns a number > 0
    - `grep -c 'CC000009.*KeyboardView' Dictus.xcodeproj/project.pbxproj` still returns > 0 (vendored file preserved)
    - `xcodebuild build -scheme DictusKeyboard -destination 'generic/platform=iOS' -quiet` succeeds with BUILD SUCCEEDED
  </acceptance_criteria>
  <done>pbxproj has zero references to deleted files, KeyboardMetrics.swift is registered in project, DictusKeyboard scheme builds successfully</done>
</task>

</tasks>

<verification>
1. All 6 old files deleted from disk (KeyButton, KeyRow, KeyboardView in Views/, SpecialKeyButton, AccentPopup, LegacyCompat)
2. KeyboardMetrics.swift exists with DeviceClass, KeyMetrics, KeySound, KeyPopup
3. Vendored/Views/KeyboardView.swift (GiellaKeyboardView) still exists
4. pbxproj has zero references to deleted file IDs
5. pbxproj has KeyboardMetrics.swift registered
6. `xcodebuild build -scheme DictusKeyboard` succeeds
</verification>

<success_criteria>
- Zero old SwiftUI keyboard files on disk
- Zero ghost references in pbxproj
- KeyboardMetrics.swift with all 4 extracted types compiles successfully
- EmojiPickerView, EmojiCategoryBar, DictusKeyboardBridge all compile (they use extracted types)
- DictusKeyboard scheme builds with BUILD SUCCEEDED
</success_criteria>

<output>
After completion, create `.planning/phases/21-cleanup-memory-profiling/21-01-SUMMARY.md`
</output>
