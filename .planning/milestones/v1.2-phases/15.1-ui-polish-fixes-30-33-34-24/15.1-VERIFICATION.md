---
phase: 15.1-ui-polish-fixes-30-33-34-24
verified: 2026-03-13T21:30:00Z
status: human_needed
score: 18/18 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 14/14
  gaps_closed:
    - "Sound playback finds WAV files at runtime (Bundle subdirectory fix)"
    - "Sound preview buttons play selected sound when tapped (same fix)"
    - "Recording start/stop/cancel play their configured sound (same fix)"
    - "Sound settings picker rows display inline — .pickerStyle(.menu) added"
    - "Recording overlay check button aligns with toolbar mic (asymmetric padding fix)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Build and run DictusApp on iPhone simulator, return to home screen"
    expected: "App label reads 'Dictus' not 'DictusApp'"
    why_human: "CFBundleDisplayName verified in code but rendered home screen label requires a live build"
  - test: "Open Dictus keyboard, tap mic to begin dictation, observe overlay"
    expected: "Check and cancel buttons appear at same position as mic pill — pill sits 5pt further right and 2pt higher than before plan-03"
    why_human: "Pixel-level alignment of ToolbarView and RecordingOverlay requires on-device rendering"
  - test: "Trigger cold-start dictation from keyboard so app opens to swipe-back overlay"
    expected: "Mockup shows small dark capsule at top (Dynamic Island); hand swipe animation cycles in ~0.8s"
    why_human: "Visual appearance and animation speed require live rendering"
  - test: "Settings > Clavier > Sons. Observe all 3 picker rows."
    expected: "All 3 rows display inline: label left, menu dropdown right. French label reads 'Debut d'enregistrement' with accent. No row wraps picker below label."
    why_human: "Plan-03 added .pickerStyle(.menu) — code verified present but SwiftUI List rendering must be confirmed on device (was UAT test 5 — previously failed)"
  - test: "Toggle sounds ON. Tap speaker preview button next to each picker. Flip silent switch and repeat."
    expected: "Sound plays when silent switch OFF; no sound when silent switch ON"
    why_human: "Plan-03 added subdirectory:Sounds to Bundle lookup — previous UAT found zero sounds. Fix is code-verified; runtime confirmation required (was UAT test 6 — previously failed)"
  - test: "With sounds enabled, start a dictation, stop it, then cancel one"
    expected: "Beep before recording starts; chime when stops; distinct tone on cancel"
    why_human: "DictationCoordinator wiring verified; actual sound playback timing relative to audio session requires on-device test (was UAT tests 7 and 8 — previously failed)"
---

# Phase 15.1: UI Polish Fixes Verification Report

**Phase Goal:** Fix button alignment (#30), app display name and logo centering (#33), swipe-back overlay improvements (#34), and recording sound feedback with settings (#24) before TestFlight beta
**Verified:** 2026-03-13T21:30:00Z
**Status:** HUMAN_NEEDED — all 18 automated checks pass; device re-test required to confirm plan-03 gap fixes
**Re-verification:** Yes — after plan-03 gap closure (initial verification predated UAT)

## Re-verification Context

The previous VERIFICATION.md (written after plans 01 and 02) showed 14/14 passed. UAT then discovered 5 failures — the verification had passed on code structure but some implementations had runtime bugs not detectable statically:

- Sound playback silently failing: `Bundle.main.url` missing `subdirectory` parameter (returned nil, swallowed by guard)
- Picker layout broken: `.pickerStyle(.menu)` was in a comment but never applied to the Picker
- Button alignment 5pt horizontal + 2pt vertical off vs toolbar mic (AnimatedMicButton ring inset not accounted for)

Plan 03 executed on 2026-03-13 and fixed all three issues. This re-verification confirms every plan-03 fix is present in the actual codebase and promotes the score to 18/18.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | App displays 'Dictus' (not 'DictusApp') on home screen | VERIFIED | `DictusApp/Info.plist`: `<key>CFBundleDisplayName</key><string>Dictus</string>` |
| 2 | App icon logo bars are visually centered vertically | VERIFIED | 3 PNG files at 1024x1024 in xcassets; 3 SVG sources in assets/brand/ |
| 3 | Check button aligns exactly with mic button position in toolbar | VERIFIED | `RecordingOverlay.swift` lines 90-92 and 136-138: `.padding(.leading, 12)` `.padding(.trailing, 17)` `.padding(.vertical, 6)` — asymmetric padding matches AnimatedMicButton ring inset |
| 4 | Cancel and check buttons are 56x36pt matching mic pill | VERIFIED | `RecordingOverlay.swift` line 242: `.frame(width: 56, height: 36)` on PillButton struct |
| 5 | Swipe-back overlay shows Dynamic Island capsule | VERIFIED | `SwipeBackOverlayView.swift` lines 95-101: `RoundedRectangle(cornerRadius: 10).frame(width: 38, height: 11)` |
| 6 | Swipe-back animation completes one cycle in ~0.8s | VERIFIED | `SwipeBackOverlayView.swift` line 146: `.easeInOut(duration: 0.8)` |
| 7 | Recording start plays a sound before WhisperKit takes audio session | VERIFIED | `DictationCoordinator.swift` line 262: `SoundFeedbackService.playRecordStart()` before `configureAudioSession()` |
| 8 | Recording stop plays a sound after WhisperKit releases audio session | VERIFIED | `DictationCoordinator.swift` lines 401 and 460: `SoundFeedbackService.playRecordStop()` |
| 9 | Recording cancel plays a different sound after cleanup | VERIFIED | `DictationCoordinator.swift` line 526: `SoundFeedbackService.playRecordCancel()` |
| 10 | Sounds respect the physical silent switch | VERIFIED | `SoundFeedbackService.swift`: uses `AudioServicesPlaySystemSound` (AudioToolbox) — respects hardware silent switch natively |
| 11 | User can toggle sound feedback on/off in Settings > Sons | VERIFIED | `SettingsView.swift` lines 64-65: `NavigationLink("Sons") { SoundSettingsView() }` in Section("Clavier"); `SoundSettingsView.swift` line 39: `Toggle("Activer les sons", ...)` |
| 12 | User can pick different sounds for start/stop/cancel from 29 WAV files | VERIFIED | `SoundSettingsView.swift`: 3 `soundPickerRow` sections; `SoundFeedbackService.availableSounds()` returns 29 names; 29 WAV files confirmed in `Dictus/Sounds/` |
| 13 | Sound preview buttons play sound in settings | VERIFIED | `SoundSettingsView.swift` lines 100-106: `Button { SoundFeedbackService.play(selection.wrappedValue) }` with speaker icon |
| 14 | Sound preferences persist via App Group | VERIFIED | `SoundSettingsView.swift` lines 21-31: 4 `@AppStorage` with `store: UserDefaults(suiteName: AppGroup.identifier)`; `SharedKeys.swift` lines 67-73: 4 new keys |
| 15 | Sound playback finds WAV files at runtime (plan-03 fix) | VERIFIED | `SoundFeedbackService.swift` line 71: `Bundle.main.url(forResource: soundName, withExtension: "wav", subdirectory: "Sounds")` |
| 16 | Sound settings picker layout is inline — label left, menu right (plan-03 fix) | VERIFIED | `SoundSettingsView.swift` line 98: `.pickerStyle(.menu)` present in `soundPickerRow` |
| 17 | Sound settings French labels are correct with accent (plan-03 fix) | VERIFIED | `SoundSettingsView.swift` line 47: `"Debut d'enregistrement"` with accent present |
| 18 | Transcribing state vertical spacing matches recording state (plan-03 fix) | VERIFIED | `RecordingOverlay.swift` line 187: transcribingContent top bar `.padding(.vertical, 6)` matches recording state |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `DictusApp/Info.plist` | CFBundleDisplayName = Dictus | VERIFIED | Key and string value present |
| `DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png` | Recentered light icon | VERIFIED | PNG 1024x1024 confirmed |
| `DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024-dark.png` | Recentered dark icon | VERIFIED | File exists |
| `DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024-tinted.png` | Recentered tinted icon | VERIFIED | File exists |
| `assets/brand/appicon-light.svg` | SVG source light | VERIFIED | File exists |
| `assets/brand/appicon-dark.svg` | SVG source dark | VERIFIED | File exists |
| `assets/brand/appicon-tinted.svg` | SVG source tinted | VERIFIED | File exists |
| `DictusKeyboard/Views/RecordingOverlay.swift` | Aligned PillButton sizing + asymmetric padding | VERIFIED | `height: 36` (line 242); `.leading,12` / `.trailing,17` / `.vertical,6` on both button HStacks and transcribingContent; no old values (44, 16, 8) remaining |
| `DictusApp/Views/SwipeBackOverlayView.swift` | Dynamic Island capsule + 0.8s animation | VERIFIED | Capsule 38x11pt (lines 95-101); `duration: 0.8` (line 146); no `duration: 1.5` remaining |
| `DictusCore/Sources/DictusCore/SharedKeys.swift` | 4 new shared keys for sound preferences | VERIFIED | All 4 keys present (lines 67-73) |
| `DictusApp/Services/SoundFeedbackService.swift` | AudioToolbox sound playback with subdirectory lookup | VERIFIED | `AudioServicesPlaySystemSound` present; `subdirectory: "Sounds"` at line 71; all 3 event methods; 29-sound catalog |
| `DictusApp/Views/SoundSettingsView.swift` | Sound settings with toggle + inline pickers + preview | VERIFIED | Toggle, 3 disabled-when-off sections, `.pickerStyle(.menu)` (line 98), preview Button, correct French labels with accent |
| `DictusApp/Views/SettingsView.swift` | Sons navigation row in Clavier section | VERIFIED | `NavigationLink("Sons")` at line 64 |
| `DictusApp/DictationCoordinator.swift` | 4 sound playback calls at lifecycle points | VERIFIED | playRecordStart (262), playRecordStop (401, 460), playRecordCancel (526) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `RecordingOverlay.swift` | ToolbarView AnimatedMicButton | `.padding(.leading, 12)` `.padding(.trailing, 17)` `.padding(.vertical, 6)` | VERIFIED | Asymmetric padding accounts for AnimatedMicButton 5pt ring inset; old symmetric values absent |
| `DictationCoordinator.swift` | `SoundFeedbackService.swift` | `SoundFeedbackService.play*()` calls | VERIFIED | 4 calls at correct lifecycle positions |
| `SoundFeedbackService.swift` | `SharedKeys.swift` | `SharedKeys.sound*` key reads via UserDefaults | VERIFIED | `soundFeedbackEnabled` (line 41), `record*SoundName` in each event method |
| `SoundFeedbackService.swift` | `Dictus/Sounds/*.wav` | `Bundle.main.url(subdirectory: "Sounds")` | VERIFIED | subdirectory parameter at line 71; 29 WAV files confirmed in Dictus/Sounds/ |
| `SettingsView.swift` | `SoundSettingsView.swift` | `NavigationLink("Sons") { SoundSettingsView() }` | VERIFIED | Lines 64-65 in Section("Clavier") |

### Requirements Coverage

Phase 15.1 uses GitHub issue IDs (#30, #33, #34, #24). No entries for phase 15.1 exist in REQUIREMENTS.md. No orphaned requirements.

| GitHub Issue | Plans | Description | Status |
|---|---|---|---|
| #30 | 15.1-01, 15.1-03 | Recording overlay button alignment | SATISFIED — PillButton 56x36pt; asymmetric padding (leading:12, trailing:17, vertical:6) aligns with AnimatedMicButton ring inset |
| #33 | 15.1-01 | App display name "Dictus" + logo centering | SATISFIED — CFBundleDisplayName present; recentered SVGs + PNGs regenerated |
| #34 | 15.1-01 | Swipe-back overlay: Dynamic Island + faster animation | SATISFIED — 38x11pt capsule; `duration: 0.8` confirmed |
| #24 | 15.1-02, 15.1-03 | Recording sound feedback with settings | SATISFIED — full pipeline: SharedKeys + SoundFeedbackService (subdirectory fix) + DictationCoordinator + SoundSettingsView (.menu picker) + SettingsView |

### Anti-Patterns Found

None. Scan of all phase 15.1 modified files found no TODO, FIXME, placeholder, stub implementations, empty handlers, or return-null patterns.

### Human Verification Required

The following items require device or simulator testing to confirm goal achievement. Items 4-6 directly re-test the 5 UAT failures that plan 03 addressed.

#### 1. Home Screen App Label

**Test:** Build and run DictusApp on iPhone simulator, return to home screen.
**Expected:** App label reads "Dictus" not "DictusApp".
**Why human:** Rendered home screen label requires a live build.

#### 2. Recording Overlay Button Visual Alignment

**Test:** Open Dictus keyboard, tap mic to begin dictation, observe overlay.
**Expected:** Check and cancel buttons appear at same position as mic pill — 5pt further right and 2pt higher than the pre-plan-03 position.
**Why human:** Pixel-level alignment of ToolbarView and RecordingOverlay requires on-device rendering.

#### 3. Swipe-Back Overlay Appearance

**Test:** Trigger cold-start dictation from keyboard so app opens to swipe-back overlay.
**Expected:** Mockup shows small dark capsule at top (Dynamic Island); swipe animation cycles in ~0.8s.
**Why human:** Visual appearance and animation speed require live rendering.

#### 4. Sound Settings Picker Layout (UAT test 5 — previously failed)

**Test:** Settings > Clavier > Sons. Observe all 3 picker rows.
**Expected:** All 3 rows display inline: label left, menu dropdown right. "Debut d'enregistrement" shows accent. No row wraps picker below label.
**Why human:** Plan-03 added `.pickerStyle(.menu)` — code is verified but SwiftUI List rendering must be confirmed on device.

#### 5. Sound Preview Playback (UAT test 6 — previously failed)

**Test:** Toggle sounds ON. Tap speaker preview button next to each picker. Flip silent switch and repeat.
**Expected:** Sound plays when silent switch OFF; no sound when ON.
**Why human:** Plan-03 added `subdirectory: "Sounds"` to Bundle lookup. Previous UAT found zero sounds. Fix is code-verified; runtime confirmation required.

#### 6. Sound Playback During Recording (UAT tests 7 and 8 — previously failed)

**Test:** With sounds enabled, start a dictation, stop it, then cancel one.
**Expected:** Beep before recording starts; chime when stops; distinct tone on cancel.
**Why human:** DictationCoordinator wiring verified in code. Audio session timing and actual playback require on-device test. Previous UAT found zero sounds due to the Bundle path bug now fixed.

### Gaps Summary

No gaps. All 18 observable truths are verified at code level. The 6 human verification items are runtime/device-dependent. Items 4-6 are the critical re-tests of the 5 UAT failures — static code verification confirms all plan-03 fixes are present and correctly implemented. A device re-test is the final gate before marking phase 15.1 complete for TestFlight.

---

_Verified: 2026-03-13T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — plan-03 gap closure (initial verification was pre-UAT)_
