# Phase 15.1: UI Polish Fixes (#30, #33, #34, #24) - Context

**Gathered:** 2026-03-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix 4 specific issues: button alignment between keyboard toolbar and recording overlay (#30), app display name and logo centering (#33), swipe-back overlay improvements (#34), and recording sound feedback with settings (#24). All are user-facing polish items that must land before TestFlight beta.

</domain>

<decisions>
## Implementation Decisions

### Button alignment (#30)
- **Reference point**: The mic button position is the reference — the check button must move to match it, not the other way around
- **Button size**: Check and cancel buttons shrink to 56×36pt to match the mic pill size exactly. Same visual weight, seamless transition
- **Layout**: Keep current layout (cancel left, check right, spacer between). Adjust positions so check button sits at the exact same coordinates as the mic button
- **Transition**: No morph animation — just ensure positions are perfectly aligned so the overlay fade/slide-in looks clean
- **Cancel button**: Adapt cancel button position if needed to maintain balance after check button repositioning

### Sound feedback (#24)
- **3 events**: Start recording, stop recording, cancel recording — each gets its own sound picker in settings
- **Sound library**: Use the 29 WAV files already in DictusApp/Sounds/ (electronic_01a through electronic_04g + ui_chime_01). All 29 available for each event
- **Default assignments**: Claude picks reasonable defaults; Pierre will fine-tune later
- **Silent mode**: Respect the physical silent switch (AudioServicesPlaySystemSound handles this natively)
- **Settings UX**: Dedicated sub-page accessed via "Sons" row in main Settings. Contains: global toggle ON/OFF, then 3 pickers (start/stop/cancel) each with play button for preview
- **API**: AudioServicesPlaySystemSound (not AVAudioPlayer) — no conflict with WhisperKit audio session
- **Timing**: Start sound plays BEFORE WhisperKit takes audio session; stop/cancel sounds play AFTER WhisperKit releases it. ~200-300ms delay between start sound and recording start
- **Bundle target**: Sounds included in DictusApp only (not DictusKeyboard — saves memory)
- **Persistence**: SharedPreferences via App Group (soundFeedbackEnabled, recordStartSoundName, recordStopSoundName, recordCancelSoundName)

### Swipe-back overlay (#34)
- **Dynamic Island**: Add to iPhone mockup with realistic proportions matching a real iPhone (not the approximate dimensions from the issue). Claude calculates correct proportions relative to the 120×260pt mockup
- **Animation speed**: Reduce from 1.5s to ~0.8s per cycle — faster, more dynamic, user understands the gesture immediately

### App name & logo (#33)
- **Display name**: Add CFBundleDisplayName = "Dictus" to DictusApp/Info.plist
- **Audit**: Full audit of project.pbxproj and all Info.plist files for any user-visible "DictusApp" occurrences
- **Logo recentering**: Shift all 3 bars up by 3px (y-3) in the SVG to center the group at y=40
- **PNG regeneration**: Claude generates all 3 variants (light, dark, tinted) at 1024×1024 from the corrected SVG

### Claude's Discretion
- Exact check button X/Y offset values to match mic position (measure from code)
- Default sound assignments for start/stop/cancel events
- Sound picker UI details (List style, preview button design)
- Dynamic Island exact proportions scaled to mockup
- SVG→PNG generation toolchain (rsvg-convert, sips, or other available tool)
- SoundFeedbackService implementation pattern

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `HapticFeedback.swift`: Centralized haptic enum with pre-allocated generators — sound feedback service should follow same pattern
- `AudioServicesPlaySystemSound`: Already used in KeyboardView for key sounds (SystemSoundID 1104/1155/1156) — same API for recording sounds
- `SharedKeys.swift`: Existing pattern for App Group shared preferences keys
- `PillButton` (RecordingOverlay): 56×44pt currently, will shrink to 56×36pt to match toolbar mic pill
- `AnimatedMicButton`: 56×36pt pill in toolbar — the position reference for check button alignment
- `SwipeBackOverlayView`: 156 lines, self-contained. Animation at 1.5s easeInOut, iPhone mockup 120×260pt

### Established Patterns
- `@AppStorage(SharedKeys.*, store: UserDefaults(suiteName: AppGroup.identifier))` for settings persistence
- `AudioServicesPlaySystemSound()` for short feedback sounds (respects silent switch)
- `withAnimation(.easeOut)` for state-driven animations
- NavigationLink for settings sub-pages (Language, etc.)

### Integration Points
- `RecordingOverlay` → button size and position changes (#30)
- `ToolbarView` → mic button position reference (#30)
- `SettingsView` → new "Sons" row linking to SoundSettingsView (#24)
- `DictationCoordinator` / `RecordingOverlay` → trigger sound playback on start/stop/cancel (#24)
- `SwipeBackOverlayView` → animation timing + Dynamic Island mockup (#34)
- `DictusApp/Info.plist` → CFBundleDisplayName (#33)
- `assets/brand/dictus-brand-kit.html` → SVG source for logo (#33)
- `AppIcon.appiconset/` → regenerated PNG files (#33)

</code_context>

<specifics>
## Specific Ideas

- Buttons: "Le bouton micro est bien positionné, c'est au check de se déplacer vers la position du micro" — mic is the anchor
- Sounds: All 29 WAV files available in every picker, Pierre will determine the best assignments after testing
- 3 sound events, not 2: start + stop + cancel (cancel was added during discussion)
- Dynamic Island: "Je veux comme un vrai iPhone" — realistic proportions, not approximations
- App name audit: check everywhere "DictusApp" might appear to the user, not just Info.plist

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 15.1-ui-polish-fixes-30-33-34-24*
*Context gathered: 2026-03-13*
