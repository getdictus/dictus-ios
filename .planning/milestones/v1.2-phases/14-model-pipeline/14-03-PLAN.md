---
phase: 14-model-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DictusCore/Tests/DictusCoreTests/ModelInfoTests.swift
autonomous: true
gap_closure: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
  - MODEL-06
  - MODEL-07
  - MODEL-08

must_haves:
  truths:
    - "All ModelInfoTests pass after Large Turbo v3 removal"
    - "Test counts reflect actual catalog: 4 available, 6 total, 5 WhisperKit"
  artifacts:
    - path: "DictusCore/Tests/DictusCoreTests/ModelInfoTests.swift"
      provides: "Updated unit tests matching current model catalog"
      contains: "XCTAssertEqual(ModelInfo.all.count, 4)"
  key_links:
    - from: "ModelInfoTests.swift"
      to: "ModelInfo.swift catalog arrays"
      via: "count assertions and identifier checks"
      pattern: "ModelInfo\\.all"
---

<objective>
Fix stale unit test assertions in ModelInfoTests.swift after Large Turbo v3 removal.

Purpose: The catalog went from 7 models (5 available) to 6 models (4 available) when Large Turbo v3 was removed in plan 14-01, but the test file was not updated. Three test methods will fail with incorrect counts and a missing model assertion.

Output: Passing ModelInfoTests with correct counts and no reference to removed model.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-model-pipeline/14-VERIFICATION.md
@DictusCore/Tests/DictusCoreTests/ModelInfoTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ModelInfoTests assertions for post-Large-Turbo catalog</name>
  <files>DictusCore/Tests/DictusCoreTests/ModelInfoTests.swift</files>
  <action>
Update three test methods in ModelInfoTests.swift:

1. `testAllContainsOnlyAvailableModels` (lines 9-20):
   - Change `XCTAssertEqual(ModelInfo.all.count, 5)` to `XCTAssertEqual(ModelInfo.all.count, 4)`
   - Remove line 16: `XCTAssertTrue(ids.contains("openai_whisper-large-v3_turbo"))` — this model no longer exists
   - Update comment on line 10 from "5 available models (4 WhisperKit + 1 Parakeet)" to "4 available models (3 WhisperKit + 1 Parakeet)"

2. `testAllIncludingDeprecatedContainsEight` (lines 22-29):
   - Change `XCTAssertEqual(ModelInfo.allIncludingDeprecated.count, 7)` to `XCTAssertEqual(ModelInfo.allIncludingDeprecated.count, 6)`
   - Change `XCTAssertEqual(available.count, 5)` to `XCTAssertEqual(available.count, 4)`
   - Update comment on line 23 from "all 7 models (2 deprecated + 5 available)" to "all 6 models (2 deprecated + 4 available)"

3. `testEngineAssignment` (lines 67-73):
   - Change `XCTAssertEqual(whisperKitModels.count, 6, "Should have 6 WhisperKit models")` to `XCTAssertEqual(whisperKitModels.count, 5, "Should have 5 WhisperKit models")`

Do NOT modify any other test methods. The remaining tests (gauge scores, descriptions, supported identifiers, labels, deprecated resolution, speech engine) are unaffected.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild test -scheme DictusCore -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:DictusCoreTests/ModelInfoTests 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 test methods in ModelInfoTests pass. No references to openai_whisper-large-v3_turbo remain. Counts are 4 available, 6 total, 5 WhisperKit.</done>
</task>

</tasks>

<verification>
- `xcodebuild test` for ModelInfoTests passes with 0 failures
- grep for "large-v3_turbo" in ModelInfoTests.swift returns no matches
- Count assertions: 4 (all), 6 (allIncludingDeprecated), 4 (available), 5 (whisperKit), 1 (parakeet)
</verification>

<success_criteria>
All ModelInfoTests pass after the Large Turbo v3 removal, confirming test coverage matches the current model catalog.
</success_criteria>

<output>
After completion, create `.planning/phases/14-model-pipeline/14-03-SUMMARY.md`
</output>
