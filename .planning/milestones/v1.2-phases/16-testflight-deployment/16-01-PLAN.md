---
phase: 16-testflight-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dictus.xcodeproj/project.pbxproj
  - DictusApp/PrivacyInfo.xcprivacy
  - DictusKeyboard/PrivacyInfo.xcprivacy
  - PRIVACY.md
autonomous: false
requirements: [TF-01, TF-02, TF-05]
user_setup:
  - service: apple-developer
    why: "Code signing with professional developer account"
    env_vars: []
    dashboard_config:
      - task: "Enroll in Apple Developer Program (Individual)"
        location: "https://developer.apple.com/programs/enroll/"
      - task: "Provide new Team ID to Claude"
        location: "Apple Developer > Account > Membership > Team ID"

must_haves:
  truths:
    - "DEVELOPMENT_TEAM in project.pbxproj uses the new professional Team ID for all 4 build settings"
    - "PrivacyInfo.xcprivacy exists for both DictusApp and DictusKeyboard with UserDefaults CA92.1 declaration"
    - "MARKETING_VERSION is 1.2 for all targets"
    - "PRIVACY.md exists with bilingual privacy policy content"
    - "App Store Review checklist covers microphone, Full Access, background audio, and URL schemes justifications"
  artifacts:
    - path: "DictusApp/PrivacyInfo.xcprivacy"
      provides: "Privacy Manifest for main app"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
    - path: "DictusKeyboard/PrivacyInfo.xcprivacy"
      provides: "Privacy Manifest for keyboard extension"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
    - path: "PRIVACY.md"
      provides: "Bilingual privacy policy"
      min_lines: 50
  key_links:
    - from: "Dictus.xcodeproj/project.pbxproj"
      to: "DictusApp/PrivacyInfo.xcprivacy"
      via: "PBXBuildFile + PBXFileReference entries"
      pattern: "PrivacyInfo.xcprivacy"
    - from: "Dictus.xcodeproj/project.pbxproj"
      to: "DictusKeyboard/PrivacyInfo.xcprivacy"
      via: "PBXBuildFile + PBXFileReference entries"
      pattern: "PrivacyInfo.xcprivacy"
---

<objective>
Prepare the Xcode project for TestFlight submission: migrate signing to professional developer account, add Privacy Manifests, bump version to 1.2, create privacy policy, and verify App Store Review compliance.

Purpose: These are blocking prerequisites for archive and upload. Without correct signing, Privacy Manifest, and version bump, the binary will be rejected by App Store Connect.
Output: Project ready to archive with correct signing, privacy declarations, and review documentation.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-testflight-deployment/16-CONTEXT.md
@.planning/phases/16-testflight-deployment/16-RESEARCH.md

@Dictus.xcodeproj/project.pbxproj
@DictusApp/DictusApp.entitlements
@DictusKeyboard/DictusKeyboard.entitlements
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Get Team ID from Pierre</name>
  <files>Dictus.xcodeproj/project.pbxproj</files>
  <decision>Pierre must provide the new Apple Developer Program Team ID</decision>
  <context>
    The project currently uses DEVELOPMENT_TEAM = QN58279822 (personal team). Pierre is enrolling in the Apple Developer Program as an Individual. The new Team ID is needed to update project.pbxproj (4 occurrences: DictusApp Debug/Release + DictusKeyboard Debug/Release).

    Pierre: go to https://developer.apple.com > Account > Membership > Team ID and share the value.
  </context>
  <action>Wait for Pierre to provide the new Team ID from Apple Developer Account > Membership.</action>
  <options>
    <option id="provide-team-id">
      <name>Provide Team ID</name>
      <pros>Unblocks signing migration</pros>
      <cons>None</cons>
    </option>
  </options>
  <verify>Pierre has provided a Team ID string (10 alphanumeric characters).</verify>
  <done>New Team ID received, ready to update project.pbxproj.</done>
  <resume-signal>Paste your new Team ID (e.g., "ABC1234567")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Signing migration, Privacy Manifests, version bump, privacy policy, and review checklist</name>
  <files>
    Dictus.xcodeproj/project.pbxproj
    DictusApp/PrivacyInfo.xcprivacy
    DictusKeyboard/PrivacyInfo.xcprivacy
    PRIVACY.md
  </files>
  <action>
    After receiving Pierre's Team ID from Task 1:

    **1. Signing migration (TF-01):**
    In project.pbxproj, replace all 4 occurrences of `DEVELOPMENT_TEAM = QN58279822` with `DEVELOPMENT_TEAM = <NEW_TEAM_ID>` (lines 690, 712, 733, 755). Bundle IDs stay unchanged (com.pivi.dictus, com.pivi.dictus.keyboard). App Group stays unchanged (group.com.pivi.dictus). CODE_SIGN_STYLE stays Automatic.

    **2. Version bump:**
    In project.pbxproj, replace all 4 occurrences of `MARKETING_VERSION = 1.0` with `MARKETING_VERSION = 1.2` (lines 694, 716, 737, 759). CURRENT_PROJECT_VERSION stays at 1.

    **3. Privacy Manifests (TF-02):**
    Create `DictusApp/PrivacyInfo.xcprivacy` and `DictusKeyboard/PrivacyInfo.xcprivacy` with identical content:
    - NSPrivacyTracking: false
    - NSPrivacyTrackingDomains: empty array
    - NSPrivacyCollectedDataTypes: empty array (Data Not Collected)
    - NSPrivacyAccessedAPITypes: UserDefaults with reason CA92.1 (App Group container access)

    Add both PrivacyInfo.xcprivacy files to project.pbxproj: PBXBuildFile entries, PBXFileReference entries, add to appropriate PBXGroup (DictusApp group and DictusKeyboard group), and add to Copy Bundle Resources build phase for each target. Follow existing pbxproj patterns for file references.

    **4. Privacy policy (TF-05 partial):**
    Create `PRIVACY.md` in repo root -- bilingual (French section first, English section second). Content must cover:
    - What Dictus does: 100% on-device voice dictation, no cloud
    - Why Full Access is needed: microphone access only from keyboard extension
    - What Dictus does NOT access: network, clipboard content, keystroke data, contacts, location
    - What data is stored locally: user preferences (App Group UserDefaults), speech model files, debug logs (no transcription content)
    - No analytics, no tracking, no third-party SDKs collecting data
    - Contact: pierre@pivi.solutions
    - Tone: detailed and transparent per user decision

    **5. App Store Review checklist (TF-05):**
    Create `.planning/phases/16-testflight-deployment/REVIEW-CHECKLIST.md` with:
    - Microphone justification: speech-to-text only, on-device via WhisperKit CoreML
    - Full Access justification: required solely for microphone from keyboard extension, no network/clipboard/keystroke access
    - Background audio justification: UIBackgroundModes:audio keeps audio session alive during cold start dictation flow, no audio playback
    - URL schemes justification: LSApplicationQueriesSchemes for auto-return to source app after dictation
    - Privacy policy: PRIVACY.md (interim URL: https://github.com/Pivii/dictus/blob/main/PRIVACY.md)
    - Reviewer test instructions (from RESEARCH.md review notes)
    - Data collection: "Data Not Collected" nutrition label
  </action>
  <verify>
    <automated>grep -c "DEVELOPMENT_TEAM = QN58279822" Dictus.xcodeproj/project.pbxproj | grep "^0$" && grep -c "MARKETING_VERSION = 1.2" Dictus.xcodeproj/project.pbxproj | grep "^4$" && test -f DictusApp/PrivacyInfo.xcprivacy && test -f DictusKeyboard/PrivacyInfo.xcprivacy && test -f PRIVACY.md && xmllint --noout DictusApp/PrivacyInfo.xcprivacy 2>&1 && xmllint --noout DictusKeyboard/PrivacyInfo.xcprivacy 2>&1 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - DEVELOPMENT_TEAM updated to new Team ID in all 4 places (old value QN58279822 gone)
    - MARKETING_VERSION is 1.2 in all 4 places
    - PrivacyInfo.xcprivacy exists for both targets with valid XML declaring UserDefaults CA92.1
    - Both PrivacyInfo.xcprivacy files referenced in project.pbxproj (PBXBuildFile + Copy Bundle Resources)
    - PRIVACY.md exists with bilingual content
    - Review checklist created
  </done>
</task>

</tasks>

<verification>
- `grep "DEVELOPMENT_TEAM" Dictus.xcodeproj/project.pbxproj` shows new Team ID in all 4 lines
- `grep "MARKETING_VERSION = 1.2" Dictus.xcodeproj/project.pbxproj` returns 4 matches
- `xmllint --noout DictusApp/PrivacyInfo.xcprivacy` passes
- `xmllint --noout DictusKeyboard/PrivacyInfo.xcprivacy` passes
- `grep "PrivacyInfo.xcprivacy" Dictus.xcodeproj/project.pbxproj` shows entries for both targets
- `cd DictusCore && swift test` still passes (no code changes)
</verification>

<success_criteria>
Project is ready to open in Xcode, build, and archive with professional signing. Privacy Manifests included in both targets. Version shows 1.2.
</success_criteria>

<output>
After completion, create `.planning/phases/16-testflight-deployment/16-01-SUMMARY.md`
</output>
