---
phase: 15-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DictusApp/Views/SettingsView.swift
  - DictusApp/Views/RecordingView.swift
  - DictusApp/Views/HomeView.swift
  - DictusApp/Views/GaugeBarView.swift
  - DictusApp/Views/MainTabView.swift
  - DictusCore/Sources/DictusCore/Design/AnimatedMicButton.swift
autonomous: true
requirements:
  - DSGN-01
  - DSGN-07

must_haves:
  truths:
    - "All French UI strings display correct accents (modele -> modele with accent, dictee -> dictee with accent, reglages -> reglages with accent, etc.)"
    - "Gauge bar for Vitesse uses blue highlight color instead of green"
    - "Mic button opacity during transcription is visually consistent in both pill and circle modes"
    - "Settings list options show native iOS press highlight (no custom buttonStyle masking it)"
    - "Log export button shows spinner during preparation"
  artifacts:
    - path: "DictusApp/Views/SettingsView.swift"
      provides: "Fixed French accents + Button-based list rows + log export spinner"
    - path: "DictusCore/Sources/DictusCore/Design/AnimatedMicButton.swift"
      provides: "Evaluated/adjusted transcription opacity"
  key_links: []
---

<objective>
Fix all French accent errors across the UI (excluding onboarding files handled in Plan 04 and RecordingOverlay handled in Plan 03), change gauge bar colors to blue-only palette, evaluate mic button transcription opacity, and improve Settings interaction feedback.

Purpose: Correct localization errors and visual consistency issues that make the app feel unpolished before beta.
Output: All French strings display correct accents in non-onboarding files (RecordingOverlay accent fix delegated to Plan 03), gauge bars use brand blue palette, mic button opacity confirmed appropriate, Settings has native press feedback and export spinner.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-polish/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: French accent audit and gauge color fix</name>
  <files>
    DictusApp/Views/SettingsView.swift,
    DictusApp/Views/RecordingView.swift,
    DictusApp/Views/HomeView.swift,
    DictusApp/Views/GaugeBarView.swift,
    DictusApp/Views/MainTabView.swift
  </files>
  <action>
Fix all French accent errors in UI strings. NOTE: KeyboardSetupPage.swift and ModelDownloadPage.swift accent fixes are handled in Plan 04 Task 2. NOTE: RecordingOverlay.swift accent fix ("Demarrage..." -> "Demarrage" with accent) is handled in Plan 03 Task 1 to avoid file ownership conflict.

**SettingsView.swift:**
- Line ~42: "Francais" -> "Fran\u00e7ais"
- Line ~67: Section("A propos") -> Section("\u00c0 propos")
- Line ~108: "Reglages" -> "R\u00e9glages"

**RecordingView.swift:**
- Line ~262: "Arreter l'enregistrement" -> "Arr\u00eater l'enregistrement"
- Line ~307: "echoue. Verifiez que le modele est telecharge" -> "\u00e9chou\u00e9. V\u00e9rifiez que le mod\u00e8le est t\u00e9l\u00e9charg\u00e9"

**HomeView.swift:**
- Line ~181: "Nouvelle dictee" -> "Nouvelle dict\u00e9e"

**MainTabView.swift:**
- Line ~69: "Reglages" -> "R\u00e9glages"

**GaugeBarView.swift:**
- Line ~49 (preview): "Precision" -> "Pr\u00e9cision"

Also change the Vitesse gauge bar color: in GaugeBarView.swift preview if it uses green, change to `.dictusAccentHighlight` (blue #6BA3FF). The call site in ModelCardView.swift is handled in Plan 02.

Do a final grep across ALL .swift files for common unaccented French words (modele, dictee, reglages, telecharge, detecte, demarrage, recommande, precision, echoue, arret) to catch any strings the audit missed. Fix any additional findings in files owned by this plan. If findings are in RecordingOverlay.swift, note them for Plan 03 but do NOT modify that file.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && grep -rn '"[^"]*Reglages[^"]*"\|"[^"]*modele[^"]*"\|"[^"]*dictee[^"]*"\|"[^"]*Francais[^"]*"' --include="*.swift" DictusApp/Views/ | grep -v "//\|test\|Test" | wc -l</automated>
  </verify>
  <done>Zero unaccented French UI strings remain in Plan 01's files (grep returns 0 matches excluding comments and tests). GaugeBarView preview uses blue accent highlight, not green. RecordingOverlay accent fix is NOT in this plan (handled by Plan 03).</done>
</task>

<task type="auto">
  <name>Task 2: Mic button transcription opacity + Settings UX fixes</name>
  <files>
    DictusCore/Sources/DictusCore/Design/AnimatedMicButton.swift,
    DictusApp/Views/SettingsView.swift
  </files>
  <action>
**AnimatedMicButton (DSGN-07):**
Read AnimatedMicButton.swift and evaluate the current transcription state behavior. The mic button already uses `opacity(0.5)` for the transcribing fill color plus a shimmer animation. Assess whether:
1. The 0.5 opacity is sufficient visual feedback (it should look clearly "busy")
2. The shimmer is visible enough
3. Both pill mode (keyboard toolbar) and circle mode (HomeView) are consistent

If current implementation is sufficient, document that finding. If adjustment needed (e.g., opacity should be 0.4 for more contrast, or shimmer speed needs change), make the adjustment. Both pill and circle modes MUST be consistent per user decision.

**SettingsView (#28):**
1. Ensure all Settings options use `Button` inside `List` (not `onTapGesture`) so they get the native iOS gray highlight on press. Check for any custom `.buttonStyle` that masks the pressed state. If using `GlassPressStyle` or another custom style that prevents the native List row highlight, either remove it or ensure it shows a visible pressed state.
2. For the log export action: replace the "Exporter" text label with an inline `ProgressView()` spinner while log preparation is happening. The spinner shows during the async log gathering, then the share sheet appears when ready. Use a `@State private var isExporting = false` flag to toggle between text and spinner.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus/DictusCore && swift test --filter DictusCoreTests 2>&1 | tail -5</automated>
  </verify>
  <done>AnimatedMicButton transcription opacity is consistent in both pill and circle modes (documented or adjusted). SettingsView options show native press highlight. Log export shows spinner during preparation.</done>
</task>

</tasks>

<verification>
1. Build both DictusApp and DictusKeyboard targets in Xcode (no errors)
2. Grep confirms zero unaccented French strings in Plan 01's files
3. DictusCore tests pass
</verification>

<success_criteria>
- All French UI strings in non-onboarding files (excluding RecordingOverlay, handled in Plan 03) display correct accents
- Gauge bar uses blue-only palette (no green for Vitesse)
- Mic button opacity is consistent between pill and circle modes
- Settings rows show native press feedback
- Log export has inline spinner
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-polish/15-01-SUMMARY.md`
</output>
