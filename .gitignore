# Xcode
build/
DerivedData/

# Archives and export options produced by scripts/ and `asc xcode archive`.
# A .xcarchive is a build output: 43 MB tracked here against a 59 MB pack, and
# every new archive overwrites it and leaves the tree dirty, which is enough to
# make cut-testflight.sh refuse and `git pull` fail. The binary that ships is
# the one in App Store Connect; the local copy is reproducible from `build/N`.
.asc/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcuserstate
*.xccheckout
*.xcscmblueprint
*.moved-aside

# Swift Package Manager
.build/
Packages/
Package.resolved
.swiftpm/

# CocoaPods (au cas où)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# OS files
.DS_Store
*.swp
*~
Thumbs.db

# IDE
.idea/
.vscode/

# Archives
*.ipa
*.dSYM.zip
*.dSYM

# Whisper models (downloaded at runtime, too large for git)
*.mlmodelc
# Except the one compiled bundle the app ships for the FluidAudio 0.15 Parakeet
# migration (#558). Temporary: removed with it before the 2.0.0 cut, see
# DictusApp/ParakeetMigration/README.md.
!DictusApp/ParakeetMigration/JointDecisionv3.mlmodelc
!DictusApp/ParakeetMigration/JointDecisionv3.mlmodelc/**
# Only ignore top-level Models/ directory (downloaded Whisper model files)
# DictusKeyboard/Models/ contains Swift source files and must be tracked
/Models/
Library/

# Python bytecode
__pycache__/
*.pyc

# Local logs + crash dumps
crash-logs.log
assets/reference/logs/**/*.log

# Design sandbox
designs/

# Playwright MCP session artifacts
.playwright-mcp/

# #268 D2 throwaway harness: a pinned ANEMLL checkout and 1.8 GB of Core ML
# weights, both fetched by tools/ane-harness/setup.sh
tools/ane-harness/.deps/
tools/ane-harness/build/
tools/ane-harness/AneBenchKit/Sources/AneBenchKit/Resources/
